# -*- coding: utf-8 -*-
r"""distill_book_skip.py —— **「哪些目录不是书」的唯一来源**（数据件 · 单一来源 · 防两处漂移）。

为什么单独成件（自伤登记 · 2026-09-23）：
  `tools_common.py` 的「已知书名」改成**数据驱动**后，会把 `投资蒸馏\` 下的**基建目录名**（如 `三闸机器化`）
  也当成"书名"⇒ 任何提到它的常驻通用工具**当场假红**（首次实现实测 **9 处假红**）。
  而 `accept7-ledger.py` 里**已经**有一份同义的排除表（`SKIP_DIRS`）。
  若各写一份 ⇒ **同一判据写两遍＝改一处等于没改**（`A-72` 家族）。
  ⇒ 提取到本件，两边都 import 它。

口径：**不是书**＝工具/实验/快照/对照类目录（它们不该被计入"册"，其名字也不该被当成书名）。
"""
SKIP_DIRS = {
    '.snapshots',        # 快照区
    'meta-router',       # 路由元技能
    '三闸机器化',         # 过程三闸的机器化工装
    '对比实验',           # 双路线对比实验（含半成品 route-B）
    '规范蒸馏对比',        # 规范对照
    '技能试点',           # 试点区
    '摘录卡',             # 摘录卡
}
