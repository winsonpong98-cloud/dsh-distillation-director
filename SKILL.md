---
name: distillation-director
description: 把一本书蒸馏成一组可执行的 Agent 技能——三闸判态制（防线3忠实度/盲测路由/达尔文体检），只判态不打分，门禁零🔴。触发：用户说「拆书」「蒸馏这本书」「蒸馏 XX」「把 XX 书做成 skill」「按手册/按流水线蒸这本书」。流程：输入决策树→阶段0整书理解→阶段1提取→阶段1.5验证→阶段2构造→阶段3链接→阶段4测试→阶段5交付；红线词分级+LLM终裁，机器层¥0先行，L2.5 单本约 ¥20。NOT for 简单总结、书评、读后感、作者人设角色扮演（后者用 huashu-nuwa）。
---

# 蒸馏操作手册 V4.9 · 升级版（三闸·门禁制）

> 本 SKILL.md = 《蒸馏操作手册》权威执行标准（2026-09-12 用户拍板）的技能化封装，正文含 §0–§16 基干 ＋ §17（V4.2 增补）＋ §18（V4.3 增补）＋ §19（V4.4 增补）＋ §20（V4.5 增补）＋ §21（V4.6 增补：强制门禁＋执行合规八条）＋ §22（V4.6.4 增补：引文型附属文件零问题表）＋ §23（V4.9 增补：通用蒸馏的十条强制项）＋ §24（V4.9.6 增补：异机装配与门禁分工）＋ **§25（V4.9.7 增补：判据语法单一来源与跨册回归）**；触发后全部执行。
>
> ⛔ **通用版声明**：本手册是**通用蒸馏技能**，适用于**任何**一本书。**手册正文、随包脚本与工具一律不得含任何一本书的数据**
> （书名／主文本名／章节名／技能 slug／行数／页锚写法／期望件数一律来自**配置或参数**）。本条依据用户 2026-09-17 明确要求
> 「**这个是通用版本，不是那本书的蒸馏版本**」，并承 AGENTS「通用件不得特定化」（`A-74`）。凡本节新增条款，一律用
> `<slug>`／`<task>`／`<技能根>`／`<册根>` 占位；自检命令亦参数化。

> ⛔ **开工第一条命令（V4.6 强制 · 不可跳过）**：`python tools\gate_start.py --task <slug>` —— **未过之前不得调用任何子代理、不得 OCR、不得写任何产物**。进入任一阶段前须跑 `python tools\gate_stage.py --task <slug> --stage N`。详见 §21。

> 底本：《蒸馏操作手册V4.0-升级版.md》（2026-09-07）；本版 = 基于《周期、估值与人性》L2.5 实测复盘（`投资蒸馏/三闸机器化/手册-v4.1-修订建议（周期估值与人性实测）.md`）的 R1–R8 修订定稿（2026-09-12）。**状态：权威执行标准（2026-09-12 用户拍板切换，替换 V4.0；V4.0 转为历史档）。**
> 一句话改动（相对 V4.0 增量）：红线词分级 + LLM 语义终裁、desc 补词与让位分离、提取器提示词模板固化（§3 附件）、提取代理分段落盘 + 超时判据改为「最后落盘时间戳」、d6 校验口径文档化、辅助脚本机器校验自检单、成本基准校准（L2.5 epub ≈¥20 级）+ 成本即时回填。
> 继承 V4.0 已验证件：机械层¥0先行、judge 输入瘦身、gate-blocked 挂起、防线3 机器扫描、L1/L2.5/L3 档位。**砍掉**：9维加权100分、三套分数并行、默认全量JSON落盘、d8 每技能全测。
> 执行强制性：未过门禁不得进下一阶段；gate-blocked/降级/豁免/轻量处理未经用户确认不得启用；独立 judge/复验默认独立子代理执行。

---

## 0.0 ⚠ 可移植性声明（**换台电脑/非原工作区时，先读这一节**）

> 本节由 2026-09-17 可移植性审计新增（《避坑手册》`A-78`）。**先说清哪些是"随插件走"的、哪些是"原工作区才有的"**，
> 免得在别的机器上把"找不到文件"误判成"手册写错了"。

### A. 随插件走、**独立可用**（不依赖任何外部目录）

| 资产 | 说明 |
|---|---|
| 本 `SKILL.md` | **方法论全文**（§0–§22）：阶段流程、三道闸、判态标准、验收口径、成本口径、红线词分级 |
| `manual-history/*.md` | 历史版本手册（回溯用） |
| `extractors/extractor-template-v4.1.md` | **提取器提示词官方模板**（阶段1 必须照它写，禁自造格式） |
| `scripts/gates/*.py` | **门禁套件（27 件，随包发行）**：`gate_start`／`gate_stage`／`gate_checklist`／`preflight`／`postflight`／`gate_selftest`／`pitfall_audit`／**`check_judge_pack`**（判官输入包派发前自检，§23.3）／**`check_install_state`**（**三层装机态闸**：落盘①／登记②／在役③，§17.10）／配置加载器 `gate_common` ／路径解析 `_paths` ／一次性配置器 `gate_bootstrap` ／工作区初始化 `init_workspace` ／其余 `check_*`／`gate_*`／`layer_quotes_gate` 等（**以目录实际内容为准**；本节只列入口件） |
| `scripts/machine_precheck_v2.py` | 9 维机器初评（¥0 本地）：`python <该脚本> <SKILL.md> [--out out.json] [--book-base <册根>]` |
| `scripts/machine_layer_readycheck.py` | 机器层"最小输入齐备度"检查：`python <该脚本> <SKILL.md> <原书册根> [--json out.json]` |
| `scripts/defense3_impersonation_scan.py` | 防线3〔操作层〕冒充扫描（¥0 本地） |
| `scripts/blindtest_lexicon_mock_v1.py` | 盲测词表初判：`--prompts <probes.json> --skills-root <技能根>`（**成对必给**）；`--self-test` 可独立自证 |

**这些脚本不读任何外部工作区路径**（2026-09-17 实测：绝对路径 0 处、包外依赖 0 处；
在**只解包**的干净目录里 11/11 项可用）。

### §0.0.1 门禁套件：**一次配置、之后零参数**（2026-09-17 新增）

> 设计目标（用户要求）：「必要的需要人工选择外，**尽量少人工选择**」。

原始版本里，这 7 个闸每个都在文件顶部写死一整套本机路径（共约 60 处）⇒ 换台电脑要改 7 个文件。
**现已全部改为读一份配置**，你只需做**一次**：

```powershell
# ① 一次性配置（能自动探测的一律不问；约 10 秒）
python scripts\gates\gate_bootstrap.py --workspace "<你的蒸馏工作区>" `
       --host main="<你的宿主工作区>"          # 只用一个宿主时给 main 即可
       # 两条线想分开管就分别给：--host fin="…\金融投资" --host edu="…\家庭教育"

# ② 自证门禁有效（故意坏样本必须被拦、好样本必须放行）
python scripts\gates\gate_selftest.py
```

之后**所有闸零参数可用**：

```powershell
python scripts\gates\gate_start.py --task <slug>          # 开工自证闸（哈希收据/条款抄录/执行单/机器层/模板）
python scripts\gates\gate_stage.py --task <slug>          # 阶段依赖链（前阶段未过 ⇒ 后阶段拒绝）
python scripts\gates\gate_checklist.py --task <slug>      # 执行单逐项（证据＋产物）
python scripts\gates\preflight.py                         # 开工前健康闸
python scripts\gates\postflight.py                        # 改后收尾闸
python scripts\gates\pitfall_audit.py --check             # 手册对账＋只读命令深跑
```

**配置自动探测的东西**（都不问你）：工作区根／原书树目录名／机器层脚本目录／避坑手册路径／
`node.exe`／`js-yaml` 目录／宿主工作区（同级目录里含 `.dsh\skills` 的）。

**缺项一律判"不适用"，不假红**：不是每个使用者都有作者的全套配套脚本（`yaml_check_generic.cjs`、
`final_acceptance.py`、`machine_scan_edu.py` 等）。缺这些时，依赖它们的检查会**明确标注不适用**，
而不会伪造一个红/绿——**这一点请在使用时留意，不要把"不适用"读成"通过"**。

**改写配置的方法**（要分开管两条线、或路径特殊时）：直接编辑 `<工作区>\.dsh\gate-kit\workspace.json`
（字段含义见 `scripts\gates\gate_common.py` 的 docstring；改完重跑 `gate_selftest.py` 自证），
或带 `--force` 重跑 `gate_bootstrap.py`（会先备份）。


### B. **只在原工作区存在**（别人电脑上没有）

§21 与 §18 里提到的 `tools\gate_start.py`、`gate_stage.py`、`gate_checklist.py`、`preflight.py`、`postflight.py`、
`gate_selftest.py`、`pitfall_audit.py` 等**门禁脚本**，以及《蒸馏工程避坑手册》`蒸馏工程避坑手册.md`
——它们属于**原作者的工程配套**，**不在本插件载荷内**。

**⇒ 在别的机器上请这样理解那几行"强制"**：

1. **门禁的"判据"随手册走**（哈希收据、条款逐字抄录、执行单逐项、阶段依赖链、防线3/盲测/达尔文三闸）——
   **判据是本手册的规范，与脚本是否在不在无关**；
2. **门禁的"脚本实现"需要你自备或重建**：`<工作区>\tools\` 下按本手册明文**自建等价校验器**
   （做什么、判什么、期望退出码，手册每处都已写明）；**自建时不许比手册松**（否则＝自造闸，见 §21.6 第 8 条）；
3. **没有脚本时不得宣称"闸已过"**：应显式降级为**人工核对＋记录证据**，并在交付说明里写明
   "本机无 gate_* 脚本，X 项以人工核对方式完成"（`A-24`：用"手工过了"代替可复核证据 ＝ 不合格）；
4. **`manual_sha256` 收据**：收据机制是"证明你读的是**当前版**手册"。没有脚本时，等价做法＝
   在本会话里**逐字抄录 ≥12 条关键条款**并声明手册版本（§21.1 的判据原文）。

### C. 三类**必须由使用者提供**的输入（插件不含、也无法预设）

| 输入 | 为什么必须你给 |
|---|---|
| **书在哪**（PDF/EPUB 路径）与**文字版/扫描版** | 决定要不要 OCR、走哪条输入决策树（§1） |
| **OCR 通道与账户**（扫描版时） | 涉及第三方 API 与费用；本手册只规定**口径**（页锚、计费字段、余额差、两段分开报），不含任何密钥 |
| **宿主工作区**（技能装到哪） | 产物落地位置由你决定（原作者的宿主目录名不构成规范） |

### D. 与原工作区口径**不同**时要显式声明的东西（避免"看起来对"）

- **原书树目录名**：原作者用 `投资蒸馏/`；别的机器可不同 ⇒ 跑 `machine_precheck_v2.py` 时用
  `set DSH_DISTILL_BOOK_TREES=<你的目录名>`（可分号给多个）或直接 `--book-base <册根>`。
- **页锚形态**：本手册/配套脚本认多种形态（`===== [PAGE n] =====`、行首 `[p<n>]`、`--- [xxx pN] ---`）
  ——**换形态要显式声明**（`A-60`：引文与锚的书写形态必须表化，尺子只认一种就恒假红）。
- **成本口径**：本手册的 `¥20.5/本` 等数字**来自原作者实测**，只作**量级参照**；你自己跑出来的数以你的账本为准
  （`A-63`：成本只有两个合法来源——计费字段或余额差；估算不得当实测）。

---
## 0. 总纲

### 0.1 触发
- 「蒸馏 XX / 拆书 / 把 XX 做成 skill」→ 走本手册
- 「对已有技能体检/完善/优化」→ 走达尔文体检（判态+修复，见 §5.3）

### 0.2 模型分工（判态制）
| 任务 | 谁做 | 说明 |
|---|---|---|
| 主流程调度/OCR/拆分/构造/写文档 | 主会话 | 同 v3.0 |
| 机械层判态（¥0） | 本地脚本 | machine_precheck_v2 / defense3_impersonation_scan / blindtest_lexicon_mock_v1 / machine_layer_readycheck；只出 PASS/🔴/▲ 清单+证据行号，**不打分** |
| 语义层判态（最小域） | 子代理 judge | 只收机器证据包（KB 级），对 🔴 候选与语义残差出「必修清单」；不读 40KB 全文 |
| 实测 d8（簇抽） | 子代理真实执行 | 红线类必测1 + 主功能簇抽1；抽测样本同时过越界五查 |
| 交付盲复核（整书抽1-2） | 独立 judge | 整书交付时盲审：只给 SKILL 全文+机器证据包，**不给主会话结论**；出「同意/异议+理由」与 A/B/C 档位 |
| ~~B 档数字分（按需）~~ **【2026-09-17 剔除】** | —（已废除） | **9 维加权数字分（darwin9-scorecard）整体废除**：依据 §5.3「达尔文体检（判态，替换 9 维打分）」／§14「权重表已废除」／§15「砍掉三分数并存」。达尔文体检的**完整形态＝机器层逐维判态 ＋ 语义层独立 judge ＋ d8 簇抽**，**不再产出 0–100 合成分**，执行单原 #19 同批剔除（历史见避坑手册 A-54） |

### 0.3 费用纪律
- 机械层零成本先行；judge 预算只覆盖语义窄域 + d8 簇抽 + 交付盲复核。
- 文字版 PDF 先跑 pymupdf；扫描版 150dpi + PaddleOCR 快扫，32B 只修问题页；避开计价高峰（周末/节假日全天低谷价）。
- **成本基准（V4.1 校准）**：L2.5 epub 文字版 ≈14 万字单本实测 ≈ **¥20.5**（2026-09-12《周期、估值与人性》：35 次 flash 子代理 + 机器层 ¥0）。估算公式**不再叠加安全倍数**（旧口径按代理次数×单价高估至 ¥30-55）。

## 1. 输入决策树（同 v3.0）
PDF→pymupdf 查文本层；无文本→§2 OCR；视频/播客→先转稿；网络文章→抓取；个人经验→经验蒸馏另册。

## 2. OCR 标准流程（扫描版，同 v3.0 §2）
渲染150dpi → PaddleOCR-VL 快扫(并发6) → 清洗 <|LOC_数字|> → 乱码检测 → 32B 精修问题页 → 复查 → 抽查。参数同 v3.0。

## 3. 阶段流程（门禁=零🔴）

### 阶段0 骨架
- 产出 BOOK_OVERVIEW.md；门禁=用户确认骨架（主旨+章节结构+技能划分）。
### 阶段1 提取
- 小书主会话直接读；大书 5 类提取器子代理分批（≤4/批）。产出 candidates/。门禁=候选池数量达标。
- **提取器提示词模板（V4.1 附件）**：一律复制 `三闸机器化/提取器提示词模板-v4.1.md`（统一格式行 + 引文硬纪律 5 条 + 宁少勿滥），禁止自造格式——消除「引文级联污染 / 候选错锚 / 输出格式不一」三类返工。
- **分段落盘（V4.1）**：提取代理**按小节分段落盘**到 notes_{band}.md，不得整文件一次性 write——主会话可轮询文件 mtime 观测进度、超时接管有依据、中途失败留部分产出。*引擎侧 write 增量（append）能力落地前，以「每写完一小节即重写该文件当前完整内容」近似；能力落地后改 append。*
### 阶段1.5 验证
- 去重合并→verified.md；盲测机器词表初判→needs_llm 子集交 judge 结构化复核（判态：误路由/无锚/邻族抢单=🔴）。
### 阶段2 构造
- 主会话写 R/I/A1/A2/E/B + 🔴CHECKPOINT + 输出结构 + 让位规则 + 医疗/危机边界。门禁=frontmatter 判态（d1 硬闸零🔴）。
- **desc 补词与让位分离（V4.1 纪律）**：判别词（触发场景/何时用）进 description 触发区；让位词（本技能转出哪些邻族技能）**只放 B1/A2/related**，禁止把邻族技能名写进 desc 触发词——否则喂词表造成邻族抢单（实测 T12 割肉曾 top=S1）。
### 阶段3 链接 → INDEX.md / GLOSSARY.md。
### 阶段4 测试
- test-prompts 每技能3条（应调用/诱饵/跨技能路由+医疗/危机）。判态见 §5.2。
### 阶段5 交付
- DIGEST.md + 安装两工作区 + 四件套 + BOUNDARIES 协议 + SUPPLEMENT。门禁=三闸零🔴 + d8 口径达标 + 交付盲复核 A/B 档。

## 4. 子代理调度与失败协议（同 v3.0 核心）
1. 机械层先行（确定性项一律本地脚本，零LLM）。
2. LLM judge 只跑语义窄域，单发小批，prompt 自包含+绝对路径+只读。
3. 提取/OCR 并发 ≤4-5；小任务主会话直接做。
4. 超时（V4.1）：判据改为「**最后落盘时间戳距今 >25 分钟**」（替代「无产出 20-30 分钟」，配合 §3 阶段1 分段落盘，主会话轮询 notes 文件 mtime 判定）→询问一次→interrupt 重派1次或换通道→连续2次无产出=该闸 gate-blocked 挂起（不是主会话降级接盘），批量跳过继续，状态机交用户。
5. 补闸只补语义窄域；机器层已放行项不返工。

## 5. 验证三闸（判态制）

每闸输出三态：**PASS / 🔴（必修）/ ▲（建议）**。门禁 = 三闸全部零🔴。▲ 由构造者登记、不强制回改；跨版本比较看「🔴数+▲数」单调下降，不做总分微调。

### 5.1 防线3 忠实度核验（硬闸）
- 机器层（¥0）：R 逐字 quote_precheck / 页锚 tv_rcheck / 池id pool_scan / 操作层冒充 defense3_impersonation_scan。命中=🔴 候选。
- 语义层（最小域）：I 层主张↔池条目语义等价 / O 层立场光谱 / 冒充候选行终裁。judge 出必修清单。
- 复验分级：judge 报 🔴/▲；仅 🔴 需「修复→同 judge 复验」闭环，▲ 登记即可。

### 5.2 盲测路由（硬闸）
- 机器词表初判 blindtest_lexicon_mock_v1 → needs_llm 子集交 judge 结构化复核。
- 判态：误路由（top1≠目标）=🔴；邻族抢单/无锚=🔴（除非证明免 LLM 机械 clear 且 top1=目标）；判停红线（时点/买卖/医疗/危机）未判停=🔴。
- **红线词分级（V4.1）**：红线词分两表——「硬指令词」（点位/何时见底/买入价/帮我决定/现在该买该卖…单独出现即判红线）与「主题词」（买/卖/抄底/崩盘…单独出现**不**判红线）。**红线命中一律 needs_llm=True 交语义 judge 终裁，机器不直接 decline**——防框架提问（值不值得下注/够不够便宜）被主题词误拦。
- **危机词表（V4.1）**：判停危机红线词补「崩盘/要崩/股灾/撤出/全撤/躲一躲/恐慌性清仓/逃」家族（区别于中性「卖」），确保危机类红线可机械命中；decline 答复须含判停速查行 + 替代动作。

### 5.3 达尔文体检（判态，替换 9 维打分）
- 机器层 machine_precheck_v2 出「逐维 PASS/🔴/▲ + 证据行号」，不打分不求和。
- 硬闸🔴（命中必修）：见 §14 硬闸表。软闸▲（命中登记）：见 §14 软闸表。
- 语义层只对 🔴 候选与语义残差出必修清单；机器已放行项不返工。
- 结论=A/B/C 档位（§13），不是 0-100 分。

## 6. 打包插件（2026-09-18 重写：**装机三步带读数**，不再是散文）

> **为什么重写**：V4.9.3 之前本节只有一行散文「… → 补 bundles → 重启生效」。
> 2026-09-18 在一台 NAS 的 DSH 容器里，正是这一步被跳过 ⇒ **装了但技能永远不出现**，
> 而且**不报任何错**（`dsh plugin add` 返回 0、目录在、`plugin list` 列得出）。
> 详见《避坑手册》`A-119`／`A-123`。**本节的每一步都必须落到一行可读的读数上。**

```
① 打包   plugin-build/<pkg>/ → tar 打包（含 index.js／cordis.patch.yml／SKILL.md／scripts／extractors）
② 安装   dsh plugin --profile <profile> add <包目录或包名或 tgz>
         ⇒ 期望：rc=0；且它**会**按安装态 reconcile（声明了 dsh.bundle.patch 的依赖自动进 bundles）
③ 复核   读三处读数，缺一不可 —— **只做①②不做③＝白忙**
         · 读数 1（登记）：profile\package.json 的 dsh.profile.bundles 里**出现该包名**
         · 读数 2（装配）：node <dsh>\lib\bin.js --profile <profile> --dump-config
                           ⇒ **看行数与命中数**（命中该插件 id ≥1），**不要看"有没有报错"**（这一层不会报错，只会静默少一行）
         · 读数 3（在役）：**重启服务**（配置在启动期组装）→ 开会话看技能目录出现该技能
                           （或按 §17.2 用引擎加载器实测）
④ 一键   python scripts\gates\check_install_state.py     # 三层装机态闸，逐层给读数与修法
```

**两条硬纪律**：

1. **`dependencies` 有 ≠ 已生效**。插件只有进了 `dsh.profile.bundles` 才会被组装、被加载、被 `apply()` 注册（§17.10）。
   `node_modules` 里有目录、`plugin list` 列得出，**都不构成"已生效"的证据**。
2. **容器/异机装机后必跑装机态闸**：`python scripts\gates\check_install_state.py`
   （`--self-test` 先自证闸有效；`--json` 出机器可读；**「不适用」与「通过」字样不同，不得混读**）。
   它的三层判据与修法见 **§17.10**。

## 7. 踩坑对照表（同 v3.0 §7，节选）
子代理大并发卡死→≤4/批+超时；PaddleOCR 乱码→清洗+检测+32B；JS 模板反引号→数组 join 或写 py；judge 长任务卡死→机器层先行+gate-blocked；机器误把引文当 AI 腔→d7 豁免 R 段/引句。
- **合并/矩阵脚本机器校验自检单（V4.1）**：写合并/解析脚本自带三项——锚归一化（sNNN vs NNN）、格式多容错（[技能=S1] / [待定] / 技能=待定…归一化）、引文命中三态（命中/错锚/无锚）。防候选格式不一、错锚漏检这类小 bug 重演。

## 8. 检查清单（判态版）
- 开跑前：查文本层 / 建 books/<slug>/+PIPELINE_STATE / 定档位 L1/L2.5/L3。
- 每阶段后：更新 PIPELINE_STATE（阶段/门禁/结论三行）/ 门禁零🔴 / 快照。
- 交付前：四件套齐全 / 三闸 gates 记录零🔴 / d8 口径达标 / 交付盲复核 A/B 档 / 账本登记（agent数/波次/估算token/实际，无 meter 标「估算」）。
- **成本即时回填（V4.1）**：每次交付把 cost-meter 实测回填蒸馏报告/讨论记录，不让「估算作废」滞后（本册已按 ¥20.51 回填三处）。

## 9. 版本管理（同 v3.0）
无 git 时用 .work/snapshot.py 快照（保留5版）；有 git 则 books/ 下 init+commit+revert。

## 10. 档位与 d8 口径
| 档位 | 适用 | d8 口径 | 单本粗估 |
|---|---|---|---|
| L1 速读卡 | 背景书/历史书群 | **允许未测**（涉红线类仍测） | ¥2-5 |
| L2.5 体系卡（默认） | 承重墙书 | **必须簇抽**=红线类必测1（时点/买卖/医疗/危机/投资路由）+ 主功能簇抽1；抽测样本同时过越界五查 | ¥15-25（实测 epub ~14万字 ≈¥20.5） |
| L3 旗舰 | 反复读的书 | **全测** test-prompts | ¥10-20（估算） |

d8 结论三态：PASS（簇抽/全测通过且越界0）/ 未测（仅 L1）/ 未过（=🔴 硬闸）。抽题覆盖不足在报告如实披露，不虚标。

## 11. 内容忠实度四防线（判态版）
| 防线 | 内容 | 判态 |
|---|---|---|
| 1 原文锚 | 无原文引用不进候选池 | 提取器 PASS/🔴 |
| 2 转述派生链 | R↔I 配对+操作层◈不冒充 | 构造自查 PASS/🔴 |
| 3 忠实度核验关 | 机器扫描+语义复核 | 见 §5.1 |
| 4 目录覆盖 | 每章零贡献须有理由 | PASS/▲ |

## 12. 会话纪律与两态落盘
### 12.1 人在场（默认）
- 一书一会话批次；会话重先开新会话；子代理只回摘要（≤3行）全文落文件。
- **落盘轻量**：每技能只落一条 gates-<slug>.json（结论+关键命中项内嵌+证据指针）；machine-report 详细 JSON 就地保留在技能目录，不往 三闸机器化/ 归档、不同步。
### 12.2 无人值守（用户不在场）
- 主会话主管按 L2.5 推进；门禁照常自动；judge 卡死→gate-blocked 挂起。
- **落盘全量**：才把 readycheck/machine-report/defense3 全量落盘到 三闸机器化/（同 v3.0 §13），作为自动续跑与事后仲裁依据。

## 13. 三闸 gates 记录（单文件判态版）

每技能一条 gates-<slug>.json，schema 如下（人在场默认形态）：

    {
      "skill": "traversal-risk",
      "档位": "L2.5",
      "fidelity3": {"结论": "PASS", "🔴": 0, "▲": 0, "关键命中": [], "证据": "<技能目录>/quote-precheck.txt"},
      "blindtest": {"结论": "PASS", "🔴": 0, "needs_llm": "3/12", "判停红线": "2/2", "证据": "blindtest-mock-v1.json"},
      "darwin": {"结论": "PASS", "🔴": 0, "▲": 2, "关键命中": ["d4 分段检查点", "d7 回声"], "证据": "machine-report-traversal-risk.json"},
      "d8": {"结论": "PASS", "口径": "簇抽 2/12", "越界": 0},
      "交付盲复核": {"结论": "A 档·可交付", "理由": "零🔴，▲2 已登记"},
      "总体": "过（零🔴）"
    }

- 关键命中项**内嵌**进本条记录（不靠指针指向可能被清掉的中间文件）；证据文件指针只指向「技能目录内就地保留」的原始产物。
- 仲裁唯一权威副本 = 本条记录 + 指针指向的证据文件。

## 14. 判态规则（硬闸/软闸，替换 9 维权重表）

判态按**后果**分硬软，不按权重数字（权重表已废除）：

**硬闸🔴（命中必修）——直接影响可用性/忠实度/安全性：**
| 项 | 判🔴 条件 |
|---|---|
| 防线3 | R 引文逐字不符 / 页锚错 / 池id 悬空 / 操作层冒充（机器命中→judge 终裁） |
| 盲测 | 误路由 top1≠目标 / 判停失败（时点/买卖/医疗/危机未判停） |
| d1 结构 | name 非法 / description 缺 何时用+触发词（→不可路由） |
| d2 工作流 | R/I/E/B 缺段 / 步骤断链（→不可执行） |
| d5 可执行 | 无 🔴CHECKPOINT / 无输出结构 / 判停速查无正文实现 |
| d7 架构 | 判停/路由多副本口径漂移 / related_skills 指向不存在 slug |
| d8 实测 | 越界输出（时点/买卖/站队）/ 显著劣于基线 |

**软闸▲（命中登记，不强制回改）——影响打磨度，不影响可用：**
| 项 | 判▲ 条件 |
|---|---|
| d1 行文 | 空话尾巴 / 超长行 >260 字符 |
| d3 失败模式 | 失败闭环不完整 / 反例不足 |
| d4 检查点 | 复杂卡缺分段检查点（简单卡单一确认点可过） |
| d6 资源 | 章节锚缺省（不影响执行） |
| d9 反例 | 反例黑名单未列（不影响本技能执行） |

门禁=零🔴；▲ 登记进 gates 记录与遗留节；跨版本比较看 🔴/▲ 数单调下降。

> **d6/d7 related_skills 校验口径（V4.1 附注）**：machine_precheck_v2 扫描范围 = 投资蒸馏树内 slug ∪ 宿主 skills 目录（v4.1 补丁已并入宿主目录扫描，彻底消除「引宿主 slug 被判实缺」占位噪音）；跨家族/宿主引用在注释带「投资家族」字样仅作可读性约定，**不再是校验依赖**。

## 15. 与 v3.0 的替换对照
| v3.0 | V4.0 | 作用保留 |
|---|---|---|
| §14 9维100分加权评分 | §14 硬闸/软闸判态 | triage/定位/比较/门禁全保留；砍掉求和排序与易错权重 |
| §15.2 三套分数并行 | §0.2 一条链+交付盲复核（A/B/C档） | 零成本初检+语义兜底+独立复核都在；砍掉三分数并存与口径差解释 |
| §13 默认全量JSON落盘 | §12 两态开关（人在场单文件/无人值守全量） | 审计靠「结论+内嵌命中+指针」；无人值守仍全量 |
| d8 每技能全测(权重23) | §10 簇抽默认（红线1+主功能1）+L3全测 | 真实调用验证+红线抓取保留（越界五查跟抽题走，不并入盲测） |
| 对外 0-100 数字分 | A/B/C 档位（B 档数字分按需） | 可交付裁决保留；具体数字为按需项 |

## 16. V4.1 修订记录（相对 V4.0，R1–R8 全采纳）
| # | 修订项 | 正文落点 |
|---|---|---|
| R1 | 提取器提示词模板作为阶段1 附件（格式统一+引文硬纪律） | §3 阶段1 + 附件 `三闸机器化/提取器提示词模板-v4.1.md` |
| R2 | d6/d7 校验口径文档化（脚本扫宿主目录为主、注释约定为辅） | §14 附注 |
| R3 | 红线词分级 + 红线命中 needs_llm 语义终裁（防框架提问误拒）+ 危机词表扩充 | §5.2 |
| R4 | desc 补词与让位分离纪律 | §3 阶段2 |
| R5 | 提取代理分段落盘 + 超时判据改「最后落盘时间戳距今>25 分钟」 | §3 阶段1 + §4 |
| R6 | 合并/矩阵脚本「机器校验自检单」成文 | §7 |
| R7 | 成本基准校准：L2.5 epub ~14万字 ≈¥20.5（估算法不再加安全倍数） | §0.3 + §10 |
| R8 | 成本即时回填制度 | §8 |

> 详细依据见 `投资蒸馏/三闸机器化/手册-v4.1-修订建议（周期估值与人性实测）.md`。**R5 的「分段落盘」引擎侧 write 增量（append）能力未落地前，以「每小节重写当前完整文件」近似，能力落地后升级。**

---

## 17. V4.2 增补：工程纪律与装机/上架（2026-09-12 实战固化 · 9 条）

> **为什么有这一节**：2026-09-12 的技能库改造链（去重收口 → 装机 → 封存回退 → 口径闭环 → BH-free 恢复）用一整天踩坑换出 9 条**工程纪律**。它们此前只散落在复盘与学习日志里，**不进制就会重复踩**（实证：同一天在同一个坑上踩了 3 次）。以下 9 条**对每一次蒸馏与每一次技能库改动强制生效**。
> 依据：`输出/今日会话复盘-2026-09-12.md` §五/§六/§九 ｜ `输出/口径登记单-2026-09-12.md` ｜ `deepseek harness/MEMORY/learnings.md` 第 7/8/9 类 ｜ `输出/防坑体系方案-2026-09-12.md`。

### 17.1 装机/上架专节（**装机不是拷文件**）

**装机四件套（缺一条即运行期出错）**：
1. **引用落地**——把新件的 `related_skills`／正文让位句改成**宿主实际存在的 slug**（否则一装就产生新死链）；
2. **承接侧回改**——回改所有**指向它**的既有技能（让位句／边界句／编排表）；
3. **硬边界去重表**——写清"谁答什么"（例：谁答"够不够便宜"、谁答"有没有资格接"）；
4. **计数文档同步**——所有写着"N 个活动技能"的文档一次性改齐。

**三前置（改运行环境前必做）**：① **逐字节快照**（如 `skills-snapshot-<日期>`）；② **先 diff 出"会动哪些文件"** 再动手；③ **留"装机后"副本**（回退≠销毁）。

**封存回退标准动作**：`封存装机后完整状态 → 用装机前快照点对点覆盖 → 逐文件 MD5 与快照比对（差异 0／多余 0）→ 引擎加载器实测件数 → 复跑四道验收闸`。**只做①（放本体）不做②（覆盖承接侧）会重新产生死链；只撤一半同样留死链 —— 二者必须成对。**

### 17.2 运行期可见性硬闸（**"写进去了" ≠ "运行期生效"**）

凡新增/修订技能内容，**必须用引擎自己的加载器实测**，不得只查文件：
`件数` ｜ `唯一名 N/N` ｜ `解析告警 0` ｜ `get()` 可取正文 ｜ `description` 在运行期可见（**只有 description 对路由可见**；`invest.md` 等同事文件在运行期是**隐形**的，必须由 `SKILL.md` 留指针）。
工具：`engine_skill_probe.mjs`／`engine_desc_probe.mjs`／`engine_desc_dump.mjs`／`skill_probe_generic.mjs`（可指定任意技能根）。

### 17.3 批量改写脚本纪律（**脚本不是事务，但必须当事务写**）

- **幂等**：写入前判断"新文本是否已在"；或让 old 串在替换后不再命中。
- **全有全无**：**先在内存里全部应用 → 跑完全部断言 → 才统一写盘**；任一断言失败即**整批拒写**（不得留半批写入）。
- **改完必跑**：`py_compile`（全部改动脚本）＋ **YAML 实解析** ＋ 口径检查；**列为交付前检查单固定项**。
- **限定作用域**：批量替换**禁止作用于 judge／证据文件**（改写证据文件＝篡改核查记录）。

### 17.4 口径变更登记单（**口径可以改，但不许悄悄改**）

**"口径差"先分三类性质，再谈修**：① **真口径分歧**（同一对象、不同算法给不同值）；② **测量对象未声明**（同一算法、测了不同目录的同名文件）；③ **证据时点不一致**（同一文件、修复前后的两个读数）。**不先分类就会把"没问题的东西"当缺陷修。**
任何"检查脚本词表／期望值／度量方式"的改动都要**登记**＋**逐行复核被改判项**。**取值一律用真解析器**（引擎／YAML），**正则只可用于诊断**；凡自写度量，必须跑一次"**全库 × 多口径对照矩阵**"暴露适用范围。
**模板**（登记到 `输出/口径登记单-<日期>.md`）：`口径名 / 权威取值方式 / 出处 / 适用范围 / 已知偏差 / 弃用的旧口径与失效件数 / 生效日期`。

### 17.5 盲测工装规范（**判官的报告先怀疑工装**）

- 喂给判官的目录**必须 `description` 全文、绝不截断**（曾把 desc 截到 260 字 → 判官读不到尾部条款 → 报了 3 条假"未收敛"）。
- 判官输入**只给"运行时可见信息"**；**禁读**测试集／期望答案／源稿树／SKILL 正文。
- 判官报告里凡出现**"截断／不可核／看不到后半段"** → **先修工装再采信结论**；并区分"设计缺陷"与"工装缺陷"，后者留痕。
- **探针的期望表必须与"当前运行态"同批更新**（状态变了而期望没变 → 每次自检都报假告警）。

### 17.6 独立验证分层（四层，逐层加严）

`机器层（¥0，确定性）` → `语义 judge（防线3）` → **`路由盲测（desc 全文）`** → `边界重复度审计`。
**凡调过 `description`／边界条款／编排表，路由盲测为必跑项**；**改了触发词或让位句后，必须**重跑**（旧报告即刻过期）。

### 17.7 同族"词面重叠"必须写**双向排除句**

同一技能族内 desc 词面重合的，**两侧都要写**"谁答度量／谁答资格与动作／谁答心法姿态"，并加**主诉判别句**（例：情绪/睡眠类主诉→心理件；纯动作主诉→纪律件；同现先定纪律再执行）。
**做法**：先用路由互斥扫描找"共现词多、无排除句"的对，**只给该分**的对补句，补完**复扫＋复跑盲测**。

### 17.8 收尾清单固定项（每本书／每批改动都要跑）

`final_acceptance`（四道验收闸）＋ `yaml_check_all`（全库 frontmatter）＋ `check_md_tables`（表格完整性）＋ `check_script_sync`（脚本一致）＋ `engine_*`（引擎口径可见性）＋ **公平回归**（改前/改后零新增**扣分** fail）＋ **快照刷新** ＋ **`learnings.md` 追加**。

> ⚠️ **公平回归必须对齐路径形状**：机器层 `d6-c2/c3/c5` 靠 `skill_path` 里的 `skills` 段反推书册根——基线放进**不含 `skills` 段**的备份目录会**造出假性新增 fail**。基线要暂存到 `<tmp>/skills/<slug>/SKILL.md` 形状再跑。
> ⚠️ **fail 要分"扣分 fail"与"信息性 fail"**：`status=fail` 但 `deduct=0` 属信息性登记，**不算回归**。

### 17.9 收尾脚本自带 UTF-8 输出保护（**假报警会让人误判"失败"**）

Python 脚本在 **GBK 控制台**下打印 `✔` 等字符会抛 `UnicodeEncodeError`，**退出码非 0 但前面所有检查其实已通过** → 属**假报警**。纪律：① 脚本内建 UTF-8 输出保护，**或**在收尾清单里写明先设 `$env:PYTHONIOENCODING='utf-8'`；② 凡"退出码非 0"，先读输出判断是**真失败**还是**编码假报警**。
（同族：**读中文文件一律用 read 工具**，不要用 shell 直接打印 —— PowerShell 输出 UTF-8 内容会变 GBK 乱码。）

### 17.10 装机态三层律（**"装上了"只认组装树** · 2026-09-18 NAS 容器实测固化）

> **由来**：2026-09-18 在一台 NAS 的 DSH 容器里装本插件，现象是**装了但不生效**——
> `node_modules` 有目录、`dsh plugin list` 列得出，但开会话时技能目录里根本没有它。
> 逐层读数后定位：①落盘层 **✔** ／ ②登记层 **✗**（`dependencies` 有它、`dsh.profile.bundles` **没有**）／
> ③在役层因此**无从谈起**。**决定性读数**：`--dump-config` 组装树 566 行里 `distillation` 命中 **0**；
> 补进 bundles ＋ 重启后同一命令 569 行、命中 **3**。详见《避坑手册》`A-119`／`A-120`。

**三层定义（缺一层即"不可宣称已装"）**：

| 层 | 名称 | 判据 | 失败时的典型症状 |
|---|---|---|---|
| ① | **落盘层** | 插件目录在、`package.json` 可解析、**声明了 `dsh.bundle.patch`**、`SKILL.md` 在场且 frontmatter 有 `name`/`description`、`index.js` 语法与**真导入**（导出了 `apply`）通过 | 报错可见（多半直接崩） |
| ② | **登记层** | profile 的 `package.json`：`dependencies` **且** `dsh.profile.bundles` 都有该包名；且 `--dump-config` 组装树里出现 `id: <去前缀名>` | **静默**：装了但不生效 |
| ③ | **在役层** | 服务可达（HTTP）／启动日志（**带时间戳**）里有服务装载行／技能根副本与包内逐字节一致 | 静默或"配置改了没重启" |

**三条铁律**：

1. **三层不可合并读数**：三层的**失败症状相同、修法完全不同**（① 重装／② 补 `bundles` 并重启／③ 排服务与技能根）。
   合成一句"没生效"＝让使用者从零猜（`A-119`）。
2. **"装上了"只认组装树**：`node_modules` 有目录、`pnpm list` 列得出、`dsh plugin add` 返回 0——**都不构成已生效的证据**。
   插件只有进了 `dsh.profile.bundles` 才会被组装 → 被加载 → `apply(ctx)` 执行 → `ctx.skills.register(...)`（`A-120`）。
3. **改配置后必须重启**：profile 配置在**启动期**组装，热改 `package.json` 不生效（症状与②完全一样）。重启前先备份，**并登记"删/改了什么"**（`A-121`）。

**仪器与闸**：

```powershell
python scripts\gates\check_install_state.py                  # 三层逐层读数 + 逐层修法
python scripts\gates\check_install_state.py --self-test      # 先自证闸有效（4 类坏样本必须被拦）
python scripts\gates\check_install_state.py --json out.json  # 机器可读
#   作者侧"本机未装插件是常态"：加 --not-applicable-ok ⇒ 打印原判但判"不适用"（不假红、不隐藏）
```

已挂 **`postflight ⑧-g`**（两条：三层装机态 ＋ 仪器自证）。

> **闭环规则（防手册腐烂）**：**每踩一个新坑，必须同批做两件事** —— ① 进《避坑手册》（现象／触发场景／立即处置／预防规则／自检命令／证据锚）；② **可脚本化的同批加进收尾门禁**。否则不算处理完。

---

## 18. V4.3 增补：防坑体系与门禁（2026-09-13 实战固化 · 37 条 ＋ 三件门禁）

> **定位**：§17 是"工程纪律 9 条"，§18 是"**防坑体系与门禁**"。二者并立、不互相替代；**与 §0–§17 同等强制**。
> **为什么要有它**：2026-09-12～13 两天里，同一个人（AI）在 **37 个**不同的点上重复踩坑——教训写在哪里都不如写进**执行标准＋可运行门禁**。

### 18.1 门禁三件套（**命令级强制，非建议**）

| 门禁 | 命令 | 覆盖 | 退出码语义 |
|---|---|---|---|
| 开工前 | `python tools\preflight.py` | ①四道验收闸 ②YAML 全库（两线）③引擎加载器实测（件数/唯名/告警/封存态）④**mtime 巡检**（有没有被别的会话动过） | `0`＝可开工；非 0＝**先处理再开工** |
| 改后 | `python tools\postflight.py` | ①YAML ②desc ≤1024（官方解析器）**②-b 余量告警（<100 字，信息性不阻断）** ③表格完整性 ④脚本同步（插件↔权威 4/0/0）**④-b 打包产物新鲜度（解包级，发行类闸）** ⑤**机器层零新增 fail**（对基线 diff）⑥`py_compile` 全脚本 | `0`＝**才算改完**；非 0＝不得宣布完成 |
| 门禁自证 | `python tools\gate_selftest.py` | 三份"故意坏掉"的样本必须被检出、一份正样本必须放行 | `0`＝门禁有效；非 0＝**门禁本身要修** |
| 手册对账 | `python tools\pitfall_audit.py --check` | 条目 6 字段齐／**只读命令深跑（逐条比期望退出码）**／无命令条目＝0／引用脚本全存在 | `0`＝手册与工装一致；非 0＝手册或工装需处置 |

**三防内建**：UTF-8 输出保护（消除"GBK 打印 ✔ 抛异常 → 假非零退出"）｜只读不写业务文件（唯一写入＝首次基线）｜统一 `期望/实测/判定` 格式。
**四不跑**（`pitfall_audit --check` 的分派）：**写入型命令不自动跑**（列出并给理由）｜**对照型命令不判 pass/fail**（`P-11`…`P-17` 是"证明坑存在"的实验，`P-13`/`P-16` **故意非零退出**）｜**扫描器不自检**｜**判官/证据文件不批量改**（`P-22`）。

### 18.2 每本书的固定动作

- **开工三步**：① 读全局 AGENTS ＋ 主 MEMORY ② **跑 `preflight.py`** ③ 看讨论记录末与 `PIPELINE_STATE`。
- **收尾必跑**：`postflight.py` **全绿**（含 ②-b 余量告警与 ④-b 打包新鲜度）；任一红项不得写"已验证/已交付"。

### 18.3 《蒸馏工程避坑手册》（37 条）

全文（含每条 6 字段：现象／触发场景／立即处置／预防规则／自检命令／证据锚）：
`蒸馏工作区\蒸馏工程避坑手册.md`。**37 条一句话索引**（正文 23 ＋ 附录 A 14）：

**P-01…P-23（工程与流程 / 环境 / 装机 / 元规则）**
P-01 "去重"只移文件不合并内容 → 让位路由指向不可加载技能 ｜ P-02 `description` 内层写裸 ASCII 引号 → YAML 解析失败 ｜ P-03 机器层基线路径缺 `.dsh\skills` 两段 → 假性新增 fail ｜ P-04 机器工具口径缺陷（宿主技能恒 fail／原稿副本当实存） ｜ P-05 审计正则漏族名＋订正注记旧号二次计入 → 池编号假阳性 ｜ P-06 把"看起来像缺陷"当缺陷 ｜ P-07 批量脚本不幂等＋失败留半批 ｜ P-08 核验锚串字形不一致（全角/半角）→ 假阴性 ｜ P-09 盲测工装截断被测字段 → 判官误报未收敛 ｜ P-10 自写正则当度量 → 长度高估 ｜ P-11 `$host` 是 PowerShell 只读变量 ｜ P-12 pwsh 不支持 heredoc ｜ P-13 `python -c` 带引号被吞 → 一律写脚本文件 ｜ P-14 `Select-Object -First N` 截断上游 → 假 `exit 1` ｜ P-15 `Get-Content` 读中文路径不牢 ｜ P-16 GBK 控制台打印 `✔` 抛异常 → 假报警 ｜ P-17 pwsh 打印 UTF-8 中文文件成乱码 ｜ P-18 `Start-Process -ArgumentList` 引号被二次解析 ｜ P-19 按命令行正则杀进程会误杀自己 ｜ P-20 装机不是拷贝，是**四件套** ｜ P-21 装机类改动缺回退点（快照＋diff＋MD5 复验） ｜ P-22 批量替换未限定作用域 → 误改 judge/证据文件 ｜ P-23 **元规则：工装伪影优先假设**（判官说"截断/不可核"，先修工装）

**A-01…A-14（2026-09-13 新增）**
A-01 "某条线专用"的硬编码路径＝工具报废 ｜ A-02 扫描仪器的"空集陷阱"（只从标记段取词 → 无该段的件永不相配） ｜ A-03 边界声明是**四态**不是两态（含 `共让位→X`） ｜ A-04 判据词表漏一种写法＝整族假警报 ｜ A-05 共享词面有相反语义（共同认领 vs 共同否定） ｜ A-06 **基线/工具副本都必须与正本同路径形状同深度** ｜ A-07 守卫断言必须按**数据形态**分派（**含"行尾风格"**） ｜ A-08 注释里的"惯例"没登记＝**悬空口径**（等于免检） ｜ A-09 补让位句必须与该件**既有触发词**逐词比对 ｜ A-10 **改权威工具 ⇒ 同批同步副本＋重建打包产物＋双门复验** ｜ **A-11 交判官前必须重新导出路由目录 ＋ 目录自带「导出时刻」**（旧目录 ⇒ 2 条假阴性） ｜ **A-12 复测期间不得在同一工作区并行试跑**（产物污染"完全独立"的主张） ｜ **A-13 技能正文的运行期可见性：spill 截断省略的是「中段」**（口径 K-20；本库恰好 3 件 >50 KB） ｜ **A-14 自报 vs 引擎观测：路由统计一律以引擎观测为准**（口径 K-21）

**§九 发行类（原 §7.3「待成条 9 项」的正式归属 · 2026-09-13 拍板）**
判据＝"**这个坑会不会在收件人那里复现**"：会 → 发行类。九项已逐项给"状态＋归属＋验收"；三条新纪律：**① 版本联动**（改权威技能/工具 ⇒ `package.json` 版本同批升位＋按新版本重建两个 tgz，**旧版本号文件保留供回溯**）**② 新鲜度入闸**（`postflight` ④-b 直接调解包级校验器；"权威改了但 tgz 没重打"从此是一次红灯）**③ 三方对齐**（权威 MD5 == 插件副本 MD5 == tgz 解包后 MD5）。
**§十 交付与台账纪律**：交付数字的**必填字段**（测的是哪棵树／口径编号／时刻）＋ 台账声明的**三件套**（命令／输出／时刻）。

### 18.4 闭环规则（防手册腐烂）

**每踩一个新坑，必须同批做两件事**：① 进《蒸馏工程避坑手册》（带**可粘贴的自检命令**与证据锚）；② **可脚本化的同批加进 `tools\postflight.py`**。**只写进聊天或只写进 learnings 不算处理完。**
对账：`python tools\pitfall_audit.py --check`（**已落地**；手册 §8.7）——扫描条目输出"6 字段齐否／有命令的／没命令的"，并对**只读型**命令**真跑并比对期望退出码**（写入型列出跳过、对照型明确标注无 pass/fail）。

### 18.5 写入型自检命令禁令（2026-09-13 事故固化成条 · 口径 V-11）

**任何"自检/验证"动作都不得覆盖交付文档**。凡脚本会写盘：① 输出路径必须**显式传参**，不得写死在脚本里；
② 运行前先确认目标路径**不是** `输出/` 下的交付件；③ 已发生覆盖的，按"备份→恢复→改工具→登记"四步处理。
> 实例（真实事故）：`machine_scan_edu.py` 原版写死输出路径且无条件写盘，一次"验证命令能否运行"把
> `输出/机器层-教育线-全量-2026-09-13.md` 从 7,425 B 覆盖成 3,523 B；已恢复并改为**必须显式传输出路径**，
> 且**覆盖前自动留底**到 `.work\overwrite-backup\`。
> **V-11 的第二半（时效要求）**：凡"以快照为唯一输入的判定"（把路由目录交给盲测判官），**快照必须在判定时刻之后重新生成**，且**目录必须自带「导出时刻」**——旧目录曾让判官产生 **2 条假阴性**（`A-11`）。

### 18.6 与《全量执行单》的挂接

《V3.1 全量执行单》"收尾固定项"已增补 **第 34–36 项**：34＝`preflight` 开工前全绿；35＝`postflight` 改后全绿（含 ②-b／④-b）；36＝门禁自证 `gate_selftest`（三坏全拦、一好放行）。

*蒸馏 V4.2 增补（§17）· 2026-09-12 · 权威执行标准的工程纪律部分：由"技能库改造链 + 装机/封存 + 口径闭环"实战固化 9 条。**与 §0–§16 同等强制**。*
*蒸馏 V4.1（升级版）· 2026-09-12 · 权威执行标准（用户拍板切换），基于 V4.0 +《周期、估值与人性》实测复盘 R1–R8；V4.0 转为历史档。*
*蒸馏 V4.3 增补（§18）· 2026-09-13 · **防坑体系与门禁**：四件门禁脚本（preflight／postflight／gate_selftest／**pitfall_audit --check**）＋《蒸馏工程避坑手册》**37 条**＋**§九 发行类**＋**§十 交付与台账纪律**＋闭环规则＋写入型自检禁令（**V-11**）＋时效要求。**与 §0–§17 同等强制**。*
*蒸馏 V4.3 补记（§18）· 2026-09-13 收口轮 · 新增 **A-11…A-14**（判官目录时效／复测并行污染／spill 只留首尾／自报 vs 引擎观测）、`postflight` 增 **②-b 余量告警**与 **④-b 打包产物新鲜度**、常驻工具本体加 **UTF-8 保护**（甲-A5 严格版）、`pitfall_audit --check` **深跑只读命令**。*

## 19. V4.4 增补：判官工作流 v2 与副本治理（2026-09-13 午后 · 教育线批9–批13＋W0 实战固化）

> 背景：教育线一天内连走批9→批13 五轮 desc 加固＋ED10–ED14 五轮独立判官复判＋「彻底方案 v2」的 W0（副本治理）／W1-a（规则清单机械抽取）。本节把**可泛化的工程经验**固化进手册；逐坑细节与自检命令见《蒸馏工程避坑手册》**v1.2（46 条）**的 A-15…A-23（§18.3 的续篇），两文互为表里：手册管"坑"，本节管"流程"。

### 19.1 判官/盲测工作流 v2（升级 §5.2／§17.5）

1. **判官输入四自证**：① 导出时刻 ② 技能根最新 mtime（导出瞬间实测）③ 件数 ④ **批次标签＋本批改动清单**（坑 A-20）。缺任一 ⇒ 判官按「不可自证」处置，相关结论**降级**；①②③由 `engine_desc_catalog.mjs` 三行头落实，④为其工装待办（§9.4 发-02）。
2. **判官判据四类**（报告必须按类计数）：**明文闭式**／**半闭**（有写法但判据不全，如"同一行为词"捕不到真近义）／**判据不可执行**（占位符、未绑定变量）／**关键事实不可自证**。
3. **计数三档并报**（主判据／窄口径／宽口径），**跨轮计数不可直接相减**——判官判据每轮都在扩类，🔴 数变化可能是"存量升格"而非"回归"；**归因纪律**：每轮 🔴 逐条归因到批次（本批引入／存量升格／判据扩类），禁止笼统报"变好/变坏"。
4. **A-11 元教训（升级版）**：判官每说一句"我无法验证 X"＝一张待办——**把 X 变成他手里就有的东西**（自证字段进工具输出，不进对判官的嘱托）。
5. **迭代边界（停线/重启规则）**：绝对 🔴 递减但靶心逐轮漂移 ⇒ 边际收益递减，且每批自带守卫自伤风险。停线＝把剩余缺口**登记为已知边界**不再追打；重启条件显式登记（如"判官报 🔴 回升"），**触发后先归因再判是否真回归**（判据扩类不算回归），不得自行改读规则、交用户拍板。
6. **结构性问题"挪动"优先于"加句"**：同一条规则散在 N 件 desc ⇒ 就地补句既耗预算也治不了根；正确方向＝判据层权威件（如 `dev-foundation-theory`）或路由权威件（W1/W3 的规则清单与第 23 件技能）。

### 19.2 desc 路由工程铁律（六条；详条见避坑手册 A-09/A-21/A-22/A-23）

1. **词面碰撞检查前置**：新增归属句前先跑双仪器＋逐词比对该件既有触发词；**同词反向豁免句与主句同批落地**（拆两批＝中间态硬矛盾）。
2. **禁占位符**：desc 里每个"→ 某件"的落点必须**逐字可寻址**；无件认领 ⇒ 点名列档＋显式程序（追问 → 兜底），或登记**有意不覆盖**（B-2 口径）。
3. **优先级句必须成对**出现或显式声明覆盖域（"两范围不相交"要写明）——单边"本句优先"＝半冲突。
4. **加句三问**：有预算吗（余 <100 必须先 replace 腾位，腾掉的原句归档不丢）？是结构问题吗（挪动优先）？归属对吗（判据层／路由权威／本件）？
5. **desc 一律单行化**（单引号包裹）：块标量会被最弱的消费方解析器吃成字面 `|`（A-15/A-16）；新蒸馏的技能**从第一天**就按单行 desc 书写。
6. **规则句必须可提取**：句式恒为「条件 → \`件名\`」；每批改 desc 同批产出/更新**路由规则清单**（W1 结构：`id / from / to / condition / priority / owner / source_quote` 逐字引用）——desc 是给人看的入口，清单是可机读的权威底账。

### 19.3 发行与副本治理（四层副本模型；升级 §6／§9／§17.1）

1. **四层副本模型**：源根（权威）→ 构建目录 → 安装实体 → 工作区装机。"改完源根"只是四分之一；教育线 W0 实测：源根走 13 轮硬化而 3 副本停在 11 天前。
2. **版本戳＋副本新鲜度闸**：源根逐件逐文件 sha256 成戳（`tools\make_edu_root_stamp.py`）；登记副本逐一比对（`tools\check_copy_freshness.py`，缺／陈旧／白名单外多余即红）。**新副本接入＝新闸登记**，且闸必须在第一次真写入上使用（A-17/A-19）。
3. **同步器四步（守卫模板）**：写前 dry-run → 落盘 → 幂等复跑（必须 0 写盘）→ **独立闸复跑**。幂等只证"重复性"不证"正确性"——写错目标层时它会在错误宇宙里自洽（自伤 #6）；误写内容**移隔离区非删除**。
4. **宣称三层，一词不混**：**落盘**（文件已写＋闸绿）／**载入**（用消费方真实代码路径端到端注册实测）／**在役**（宿主重扫实证）。不得自行重启引擎/宿主等在役层刷新。
5. **与发行链的关系**：§6/§9/§17.1 的"版本联动＋新鲜度入闸＋三方对齐"管**送出去的**（tgz）；本节管**跑起来的**（装机副本）。两链同闸哲学：**权威改了，任何下游没跟上都是红灯**。

### 19.4 V4.4 修订记录

- **R1** 判官输入四自证＋判据四类＋计数三档并报＋归因纪律（来源 ED10–ED14 五轮实测）。
- **R2** 停线/重启规则与"挪动优先"（来源 ED13/ED14 复盘与停线声明）。
- **R3** desc 路由六铁律＋规则清单化（来源批9–批13＋W1-a；A-21 检测器首跑即抓到第 2 处占位符 `dev-<年龄段>`＝`dev-methods-check`，W2 批清零）。
- **R4** 副本治理四层模型＋版本戳＋新鲜度闸＋同步器四步（来源 W0，含自伤 #6 全过程）。
- **R5** 宣称三层（落盘/载入/在役）。
- **R6** **包版本对齐规则**：本版起包版本 minor 对齐手册增补号——4.2.2 → **4.4.0**（§18 的 V4.3 内容当时并入 4.2.2 发行，4.3.x 跳过不用；两个旧版本 tgz 保留供回溯，A-10 口径不变）。
- **R7** H1 标题 V4.1 → V4.4（此前已落后 §17/§18 两轮增补）。

*蒸馏 V4.4 增补（§19）· 2026-09-13 午后 · **判官工作流 v2 与副本治理**：判官输入四自证／判据四类／三档并报＋归因／停线重启规则／desc 路由六铁律＋规则清单化／四层副本模型＋版本戳＋新鲜度闸＋同步器四步／宣称三层。**与 §0–§18 同等强制**。*

## 20. V4.5 增补：底账同代、判官输入工程与闸的升格秩序（2026-09-13 晚 · 教育线批14–批16＋W5/ED15 实战固化）

> 背景：「彻底方案 v2」后半程——批14（11 件 24 处瘦身＋A-21 清零）→批15（术语判据表 T-1…T-9）→批16（postflight ⑧ 四闸挂入）→W5（双仪器复扫＋在役层实测＋ED15 终判 **🔴7→0，停线维持**）。本节把当天实证的六类可泛化经验固化为强制条款；守卫自伤 #7/#8、判官三次派发链条均已如实登记（《避坑手册》A-07 第 8 形态等）。

### 20.1 底账同代铁律（R8）
- 任何从源根**机械派生**的底账（规则清单、抽取 json、路由目录、缓存表）必须与源根**同批同代**：源根 desc 一变 ⇒ 同批重派生＋重校验；跨批不重派＝漂移隐患。
- 底账漂移**必须由独立闸抓、不能靠手气**：判据＝"底账中每条引用（quote/锚点）仍是源根的逐字子串"。教育线实证：批14 改 desc 后未重抽，validator 首跑 **44/66 红**当场抓住，同批重建后 **66/66** 回绿。
- 已固化：postflight ⑧ 第 4 闸 `rules-v1 底账同代`（66/66 quote 子串）——凡同类底账一律照此挂闸，闸红即"清单落后于源根"，先重派生再收尾。

### 20.2 守卫与脚本纪律（R9 ＝ A-07 第 8 形态）
- 守卫断言必须**按数据形态分派**；第 8 形态：**新串与文件既有文本同文**（replace 的目标文本恰好等于该文件另一处既有文本）⇒ 命中态断言写「todo＝旧1＋新1／done＝旧0＋新2」，禁用"新串必须唯一"（批15 dev-young-adult「18-34 岁」与尾句同文实证）。
- **改任何权威工具/脚本，先语法自检再运行**：`.mjs` 先 `node --check`、`.py` 先 `py_compile`；手工编辑增删块导致的收括号丢失由语法自检当场拦（批16 吞括号实证，今日自伤 #8）。

### 20.3 闸的升格秩序：先平账、后拧闸、再自证（R10）
1. 新只读检查工装先**独立跑绿**（双根全净，如 A-21 占位符扫描双根 22＋44 件）；
2. 再挂入 postflight 为 rc=0 闸（⑧ 教育线四闸：副本新鲜度 A-17／desc 占位符 A-21／引擎探针 22/22／**rules-v1 底账同代**）；
3. `gate_selftest` 复证门禁自身有效性不受影响（三坏全拦、一好放行）；
4. **闸数与文档计数同批同步**：postflight 结论行（现为"15 项检查·含⑧教育线四闸"）＋《避坑手册》§8.2 计数（现为"7 组 15 项"）——改闸不改文档＝悬空口径。

### 20.4 判官输入工程（R11，ED15 第五节全盘固化）
- **载荷内联**：件名必带正文（desc/quote 逐字全文）；只有件名＋字数＝**主会话输入准备失误**（ED15 ▲-1/▲-2 实证：判官拒臆测、拒以件名推断语义——拒判是纪律的正确执行，不是故障）。
- **派发前自检闸**（dispatcher 逐项打勾后才能发出）：件名↔正文成对率 100%；哈希目标**绝对路径**在包内；判据表（或本片相关子集）在包内；上轮基线摘要 ≤1KB 且标注"仅供对照，不作预期"；判官特权清单明列（建议：Get-FileHash 1 次＋源根 SKILL.md Get-Item(mtime) 1 次）。
- **分段落盘强制**：判官第一条动作＝把报告骨架（章节标题＋每节一句结论）落盘，此后每完成一节追加落盘；失败模式从"全有全无"降为"至多丢一节"。
- **机械层 stdout 前置**：js-yaml 解析、validator 输出、引擎探针计数随包附上——判官从"推导者"改回"裁决者"（只做抽查＋语义层），轮次预算降一个量级。
- **预算与分片**：提示内写明工具调用 ≤N、墙钟上限、70% 预算即停推导落盘已核部分；**输入包 ≤12KB/片**，超出即分片（语义判官×2＋链与规则判官×1，主会话拼接各片报告）。
- **中断处置序**：长跑无落盘 → 发"分段落盘"定向推进 → 仍无产出 → interrupt → 复派；**复派精简的对象是输入包，不是任务简报**（ED15 二次派发实证：简报短了输入没短＝重新付出全量成本）。
- **分栏记账**：判官输入链 ▲ 与被检对象 desc 层 ▲ **分栏登记**，不得混入同一栏目污染路由质量台账（ED15 ▲7 条中 0 条归因技能库本体）。

### 20.5 时效链与哈希互证（R12，发-05 定案）
- 判官输入目录头自带七行：批次标签＋本批改动＋mtime 采集范围＋防护声明＋时效结论＋件数双源＋**源根版本戳摘要 sha256[:16]**（判官可 certutil／Get-FileHash 自行重算＝**非时钟同源**第二来源；ED15 判官侧首测通过，`4390487c9e9f1dcf` 三源一致）。
- **边界声明（ED15 精确化）**：哈希补的是**数值转录层**第二来源，**不补时间真值层**（若采集 mtime 时出错，哈希会忠实锁定错误值）——每份判官报告继续携带此边界，直至补独立时间源（授权判官对源根 stat 一次即闭合）。
- 目录头加**批次溯源指针行**：现行目录文件名＋其哈希＋上一版目录文件名＋其哈希——判官可追溯而不塞全史（解 ED15 ▲-6"累计改动未复述"）。

### 20.6 在役层三层排障序（R13）
- 症状（列表显示旧值／缺件／`|`）→ 按层定位，**禁止未定位先改文件**：①**磁盘态**（副本版本号、件数、desc 格式、mtime）→②**载入态**（全新 require＋`apply(ctx)` 实测注册计数与 desc 内容）→③**在役态**（运行中进程的注册快照）。
- 磁盘态新＋载入态新＋仅运行进程旧 ⇒ **零文件改动**，处置＝进程重载（会话/应用重启）；实证：批14 后本会话目录仍显 13 件 `|`＋缺 6 件，全新加载 22/22 全量且含批15 修复——"复发"多数是快照陈旧而非文件陈旧。

### 20.7 并发会话共享文件纪律（R14）
- 多会话并行时共享长日志（讨论记录／台账）会互相撞 `edit`（file changed since read）：**用 read 工具刷新末尾后立即定点追加**，锚点用当前末行原文；撞车即重读重试；**禁止 read(limit)+整体 write**（原有纪律不变）。
- 发现并行会话痕迹（尾部出现陌生小节）→ 在台账**交叉登记**其范围与前置条件（如：并行会话已完成第 23 件内容层、其装机前置＝"W5（ED15）收尾"），避免重复劳动与互踩。

### 20.8 V4.5 修订记录
- **R8** 底账同代铁律＋postflight ⑧ 四闸（11→15 项检查）。
- **R9** 守卫断言第 8 形态（新串与既有文本同文）＋"改工具先语法自检"。
- **R10** 闸的升格秩序：先平账、后拧闸、再自证、计数同批同步。
- **R11** 判官输入工程八条：载荷内联／派发前自检闸／分段落盘强制／机械层 stdout 前置／预算与 ≤12KB 分片／中断处置序／复派精简输入包／分栏记账。
- **R12** 哈希互证入目录头＋时效边界声明（哈希不补时间真值层）＋批次溯源指针行。
- **R13** 在役层三层排障序（磁盘态→载入态→在役态；快照陈旧零文件改动）。
- **R14** 并发会话共享文件纪律（read 刷新后定点追加＋交叉登记）。
- **R15** 包版本对齐（R6 规则）：4.4.0 → **4.5.0**（双 tgz 同批重打包＋双门复验）。
- **R16** H1 标题 V4.4 → V4.5。
- **候选修正（待用户拍板，未生效）**：停线规则①加归因限定——"判官报 🔴 回升**且含当批引入项**"才触发重启；纯判据扩类／存量升格记台账不触发（ED15 报告 §四建议）。

## 21. V4.6 增补：强制门禁 —— 把"按手册执行"从自觉变成命令级拦截（2026-09-13 用户指令固化）
> ⚠️ **读全文再抄录（2026-09-17 外部实测反馈）**：`skill` 工具返回的正文**会被引擎裁剪**（实测一次省略 **13,491 字节**，全文落在 spill 文件里）。而闸①要核**手册哈希**、闸②要**逐字抄录 ≥12 条条款**——**只拿裁剪版去抄，中部条款会缺失、逐字必然对不上**。
> 正确做法：**先读全文**（直接读插件的 `SKILL.md`，或读 spill 里那份全文），**再**执行抄录与哈希核对。


> **为什么有这一节**：2026-09-13 ADHD 专业线实战中，执行者**未读本手册全文（只看了 AGENTS 注入摘要）、未读《蒸馏工程避坑手册》46 条、自造提取器格式（官方模板一直在却没用）、跳过阶段0／1.5／4／5、未跑任何 ¥0 机器层脚本、误用已废除的 9 维打分口径**，导致三轮返工、且构造层的 over-claim 已流入输出。用户指令原话：**「做成门禁，让偷懒跑不通」**。
> 本节与 §0–§20 **同等强制**；它管的不是"知识"，是**流程合规**。

### 21.1 开工强制闸（命令级 · 不可跳过）

| 闸 | 命令 | 拦截什么（全部机器可校） | 退出码语义 |
|---|---|---|---|
| **开工自证闸** | `python tools\gate_start.py --task <slug>` | ①`read_receipt.json` 的 `manual_sha256` ≠ 当前手册哈希（＝**未读或读了旧版**）；②抄录条款 <12 条或有条款在手册中**逐字找不到**（＝"我读过"不可信）；③`.work/<slug>/执行单.md` 不存在或形态不符；④`machine-layer-run.json` 不存在（＝¥0 机器层未跑）；⑤提取器 prompt 不含官方模板格式行（＝自造格式） | 非 0 ＝ **不得开工** |
| **阶段门禁闸** | `python tools\gate_stage.py --task <slug> [--stage N]` | 各阶段**产物缺失或内容不达标**；且**前阶段未过 ⇒ 后阶段命令直接拒绝**（阶段依赖链） | 非 0 ＝ **不得进入该阶段** |
| **执行单逐项闸** | `python tools\gate_checklist.py --task <slug>` | 执行单 36 项**逐项**：①"完成日期"列非空（＝V3.1 纪律 1 的"记证据"可机查）；②登记于 `ARTIFACT_MAP` 的 27 项产物在任务目录 glob 到非空文件（自查表/独立 I/O/防线4/d8/账本等**历史上被跳过的尾段产物全在射程**）；例外仅认执行单 §4 例外登记表（"用户确认"非空）。产物名含 `<slug>` 自动替换；跨目录记录在 `.work/<slug>/checklist-roots.json` 声明（声明即登记）。校准用 `--report-only` | 非 0 ＝ **不得宣称"执行单已完成/闸已过"** |
| **自动纳入** | `python tools\preflight.py` 第 ⑤ 项 | **活跃蒸馏任务**（`PIPELINE_STATE.md` 在 24h 内更新过）**自动纳入**；任一活跃任务未过上述三闸 ⇒ preflight 非零退出。**无需声明、无法手改规避** | 非 0 ＝ 开工前门禁未过 |

### 21.2 两条铁律

1. **蒸馏任务的第一条命令必须是 `gate_start.py`**；未过之前**不得调用任何子代理、不得 OCR、不得写任何产物**（含 PIPELINE_STATE 之外的一切）。
2. **进入任一阶段前必须跑 `gate_stage.py --stage N`**；它报红时，唯一合法动作是**回头补前置阶段**——不得"先做着后面再说"，不得以"时间紧/用户催"为由跳过。

### 21.3 阶段-产物-校验对照表（`gate_stage.py` 的实际判据）

| 阶段 | 必需产物 | 内容达标判据（空文件糊弄不过去） |
|---|---|---|
| **0-骨架** | `BOOK_OVERVIEW.md` | ≥3 个章节标题 ＋ 含**「用户确认」标记**（§3 阶段0 门禁＝用户确认骨架） |
| **1-提取** | `extractor-prompts/*`（须含官方模板格式行）＋ `notes/` 或 `candidates/` | 官方格式条目（`### ` 行）合计 **≥5** |
| **1.5-验证** | `verified*.md` | 候选条目／表格行 **≥5** |
| **2-构造** | `skills/*/SKILL.md` | 含 **🔴CHECKPOINT**（§14 d5 硬闸：无 CHECKPOINT＝🔴）＋ 含**「何时不用／让位」**段 |
| **4-测试** | `test-prompts*.json` | **≥3 条**（应调用／诱饵／跨技能＋医疗危机） |
| **5-交付** | `gates-<slug>.json` ＋ `DIGEST*.md` | 三闸记录 ＋ 四件套 |

### 21.4 与既有闸的关系（分工，不重叠）

- `preflight`（§18.1）＝**工作区健康**（四道验收闸／YAML 两线／引擎加载器／mtime 巡检）；
- 本节 `gate_start` / `gate_stage` ＝**蒸馏流程合规**（手册读没读／执行单建没建／机器层跑没跑／阶段有没有跳）；
- 两者在 `preflight` 第 ⑤ 项汇聚：**任一红即不得开工**。

### 21.5 自伤登记（本节诞生原因，留痕供后来者对照）

| 违规 | 后果 |
|---|---|
| 未读本手册全文，只看了 AGENTS 摘要 ⇒ 以为"知道手册" | 跳过阶段0/1.5/4/5，缺失全部门禁 |
| 未读《避坑手册》46 条 | 重复踩工程坑；`pitfall_audit --check` 从未跑过 |
| 官方提取器模板存在却自造格式 | 提取笔记与官方格式不兼容 ⇒ 阶段1.5 机械去重**执行不了** |
| 未跑 ¥0 机器层（machine_precheck／defense3／blindtest_lexicon／readycheck） | 花了 9 个子代理做人工核验，仍漏项 |
| 误用**已废除**的 9 维打分口径（§5.3 明写"判态，替换 9 维打分…结论＝A/B/C 档位，不是 0-100 分"） | 门禁结论**不成立**，须按判态制重跑 |
| 构造层无独立核验（核验只覆盖提取笔记） | **over-claim 流入输出**（"没有证据表明"被写成"无效"），由达尔文判官实测发现 |

### 21.6 执行合规八条（用户指令「把这 8 招落实到插件」· 2026-09-13 晚）

> 模型如实声明"不能保证以后都按流程走"是**诚实且正确**的——技能＝提示词级流程而非运行时强制，门禁只查"已产出的东西"（遗漏无产物、无属性可查）。本节把分散的执行合规条款收拢为八条，逐条写明**承接结构**（闸／手册节／行为规则）与**检查方式**；设计原则一条：**不依赖模型自觉，每条都有外部结构承接**。线A/B/C 与一切子代理任务同样适用。

| # | 条 | 强制要求（一句） | 承接结构 | 检查方式 |
|---|---|---|---|---|
| 1 | **主管制** | 主会话当主管、逐段对账；子代理只派刀尖任务，**禁止把整书打包给一个子代理自由发挥**；信落盘、不信转述 | §12（无人值守模式）＋本节两闸须由主管在会话内执行 | 子代理回报 ≤3 行摘要＋全文落文件；阶段放行以主管核对落盘产物为准 |
| 2 | **执行单制** | 开工复制全量执行单到 `.work/<书slug>/`，逐项记证据；偏离（降档/豁免/代核/轻量）停下经用户确认登记 | **gate_start 闸③（存在/形态）＋gate_checklist 逐项闸（36 项证据＋27 项产物）**＋ V3.1 纪律 1/2 | `gate_start.py` 与 `gate_checklist.py` rc=0 |
| 3 | **闸前置制** | 机器层 ¥0 先行；每阶段收口 rc=0 才进下一阶段；无"先往下走回头补" | **gate_start 闸④⑤＋gate_stage 阶段依赖链＋preflight ⑤ 自动纳入** | 两闸＋preflight rc=0 |
| 4 | **独立制** | 独立 judge/核验员默认独立子代理（未参与产出）；机器命中不因 judge 翻案 | V3.1 纪律 3（行为规则层） | judge 子代理与产出者不同源；台账记录执行者 |
| 5 | **少做清单制** | 每阶段回报**固定含"我没做什么"**；缺节＝汇报不合格退回重报 | gate_start 闸②（逐字抄录）防"没读装读"；自曝漏项属行为规则层 | 回报含"少做清单"节＋与落盘对账相符 |
| 6 | **落盘对账制** | 分段落盘＋时间戳；阶段产物按固定模板落盘 | **gate_stage 产物＋内容双达标**（空文件糊弄不过去，如阶段2 须含 🔴CHECKPOINT 与"何时不用"段） | `gate_stage.py` rc=0 |
| 7 | **一书一会话制** | 一本书＝一个新会话；压缩后 ≈1MB 或 ≈2,000 帧即换会话（旧会话保留） | §12 纪律＋`yieldfix.mjs verify` | 续跑前查体积两指标 |
| 8 | **可验证化制** | 每句"我无法验证 X"＝登记待办，下一批把 X 变成手里就有的东西 | A-11 元教训＋§20.4（派发前自检闸） | 台账待办栏销账记录 |

> 八条中 **2/3/6 已闸化**（gate_start／gate_stage／**gate_checklist**／preflight ⑤——第 2 条的"逐项证据＋产物存在"由执行单逐项闸机查），**1/4/5/7/8 为行为规则层**（由主管制与台账承接）。任何一条失效＝流程事故，按《避坑手册》对应 A 条登记。

*蒸馏 V4.6 增补（§21）· 2026-09-13 · **强制门禁**：`gate_start`（手册哈希＋条款逐字抄录＋执行单＋机器层＋模板）／`gate_stage`（阶段依赖链）／preflight ⑤ 自动纳入活跃任务。**与 §0–§20 同等强制**；包版本 4.5.0 → **4.6.0**（R6/R15 对齐规则）。*

*蒸馏 V4.6 修订（§21.6）· 2026-09-13 晚 · **执行合规八条对照**（用户指令「把这 8 招落实到插件」）：主管制/执行单制/闸前置制/独立制/少做清单制/落盘对账制/一书一会话制/可验证化制——2/3/6 已闸化，1/4/5/7/8 行为规则层；包版本 4.6.0 → **4.6.1**（同增补内修订＝patch 位）。**与 §0–§20 同等强制**。*

*蒸馏 V4.6 修订（§21.1/§21.6）· 2026-09-13 晚 · **执行单逐项闸建成**（用户拍板「现在做，我有时候不值守」）：`tools\gate_checklist.py` 把八条第 2 条彻底闸化——36 项证据闸＋27 项产物闸（射程＝《最优解人生》事故跳过的全部尾段产物）；挂入 preflight ⑤；`gate_selftest` 升四坏两好（自伤 #9＝填充正则 `\s` 吞行间换行并行，自证当场拦）。产物映射与证据类豁免清单在脚本头**登记为口径**。包版本 4.6.1 → **4.6.2**。**与 §0–§20 同等强制**。*

*蒸馏 V4.6 修订（§21.1 工具链同步）· 2026-09-13 深夜 · **ADHD 专业线收官批：三处机器层工具缺陷修复同批入包**——① `defense3_impersonation_scan.py` 页锚**形态表化**（旧版写死投资线 `〔…（p7）〕` ⇒ 书籍线 `【…PDF pNNN】` 永不匹配 ⇒ **suspects 恒为 0、闸恒空转**；修后 `--selftest` 加 3 例正样本对照，10/10 过）② `blindtest_lexicon_mock_v1.py` **真实现 `--prompts`＋`--skills-root` 成对开关＋`--out`**（旧版开关只在 docstring、代码无条件用内嵌 fixture ⇒ 换书跑盲测产出**别的书的证据**）③ `machine_precheck_v2.py` 的 `d1-c3` **标记词家族匹配**（`何时用`／`何时不用`／`让位`／`缺省优先`；`触发词` 仍为独立必需项）。配套新仪器 `tools\verify_layer_quotes.py`（引文型附属文件的**引文回源**：省略号分段＋归一化含 markdown 强调符＋双源＋空集拒跑）。三条坑入册《避坑手册》**v1.9（62 条）** A-36…A-39。包版本 4.6.2 → **4.6.3**（同增补内修订＝patch 位）。**与 §0–§20 同等强制**。*

*蒸馏 V4.5 增补（§20）· 2026-09-13 晚 · **底账同代、判官输入工程与闸的升格秩序**：ED15 终判 🔴7→0（停线维持）当日实战固化。**与 §0–§19 同等强制**。*


---

## 22. V4.6.4 增补：判官要求固化 —— 「引文型附属文件」零问题检查表（2026-09-14 · ADHD 专业线收口批）

> **为什么有这一节**：一条专业线上，**六名独立判官**（C／E／F／G1／G2／G3）在同一套"引文型附属文件"上
> **推翻执行者仪器 6 次**——每次都不是内容出错，而是**执行者自己的尺子**：把多引用锚块当单引用（串书/丢页）、
> 区间锚只取首页、归一化漏 `『』`、跨页只拼不重叠、位置配对取错锚、计数只报两类。
> 每一次修正都由"判官说了执行者无法反驳的话"触发。**本节把这些要求从"经验"升级为"命令"**。
> **与 §0–§21 同等强制。**

### 22.1 零问题机检闸（**rc=0 才算零问题**）

| 闸 | 命令 | 覆盖的判官要求（每条对应一次真实自伤） |
|---|---|---|
| **引文型附属文件零问题闸（通用版 · 任何册）** | `python tools\layer_quotes_gate.py --task <slug>`（锚形态／页源／引文取法读当册配置 `.work/<slug>/layer-quotes-<slug>.json`；缺配置拒跑） | **R1** 锚块全部可解析（不可解析＝**登记配额，只许减不许增**，A-51）｜**R2** 引文全覆盖（无留白）＋**R2b 射程排除带上限**｜**R3** 分栏自洽（各栏之和＝总数，A-42）｜**R4** 归一化族齐全（`『』`／全半角／空白／强调符，A-04 家族）｜**R5** **未命中零未处置**（每条必须归类，禁"未分类"）｜**R6** 判定用**行内锚并集**（A-49）｜**R7** 空集拒跑（A-37）｜**R8** 产物带 schema＋口径全字段（A-46） |
| 教育线专用闸（**未迁移 · 自声明**） | `python tools\gate_layer_quotes.py --task adhd-pro [--strict]` | 同 R1–R8，但**锚形态写死教育线**（书别名锚＋每页一文件）；教育线的通用闸配置为 `gate_ready=false`，其迁移需先实现 `miss_classes`（§22.4） |
| **通用闸的当册配置模板** | `tools\layer-quotes-config.template.json`（拷成 `.work/<slug>/layer-quotes-<slug>.json`） | 锚形态／页源模型（`single_file_pagemarks`｜`file_per_page`）／引文取法／射程规则——**数据住当册，`tools\` 零册别数据**（A-74） |

`--strict`：把"长引文待复核"也视为红（交付前建议开）。

### 22.2 判官要求的十条（写作与核验时的硬约束）

1. **禁把"多引用锚块"当单引用**（A-48）：一个 `【…】` 可含多引用（`…；AAP 第7章, PDF p112, p115, PDF p128`）⇒
   **先按 `；` 切段 → 每段取最近书别名（无则继承前段）→ 页说明取第一个 `PDF` 之后**（`split('PDF')[-1]` 会丢前面页）。
2. **锚的页说明必须区间感知**（A-44）：`pN–pM`／`pN、pM` 都要展开；只取首页会把**已正确的区间锚**误判成"跨页未声明"。
3. **归一化族必须齐全**（A-04 家族）：`『』`／`“”`／`「」`、**全/半角括号**、空白、**markdown 强调符**（`*`/`_`/`` ` ``）
   一律先归一再比对——判官 G2 实测：**8 组仅靠前两步即可当场转正**。
4. **比对必须声明窗口并穷举命中模式**（A-41/A-47）：单页／**相邻页拼接**／**分段归页**／**页界重叠**；
   **窗口未声明时不得给出「不存在」结论**。
5. **"引文↔锚"不做位置配对**（A-49）：判定问的是"这条引文在**这行声明的来源**里有没有原文"，
   用**该行全部锚的 `(书,页)` 并集**；"它属于哪个锚"是**判官的定位工作**，不是判定前提。
6. **取样一律用 `(书, 页)` 二元键并回显书代号**（A-45）：按页号取会串书（实测 `[bark p128]` 被当成 AAP 第128页）。
7. **`old` 串唯一性是执行前提**（A-43）：批量改写必须先断言"该串在原位唯一"，断言不过**整批拒写**；
   dry-run 的"跳过率异常高"要当**结论**看（形态不支持自动改写），而不是去放宽参数。
8. **计数必须给完整分栏**（A-42）：任何分类计数都要让"各栏之和 ＝ 总数"当场可验；
   **配对/解析派生的计数不可跨版本相减**（A-46），只能作同版内相对诊断，**禁止当"缺陷数"对外报**。
9. **缺书代号的锚不猜改**（A-51）：只补"同行唯一书"可继承者；其余**登记配额（只许减不许增）**，
   核验时以**该文件头声明的来源书**作回退——**写错书代号比缺更糟**。
10. **判官说"我无法验证 X"或"你的机器数不可信"⇒ 一张必办待办**（A-11 元教训）：
    把 X 变成他手里就有的东西（自证字段进工具输出，不进对判官的嘱托）。

### 22.3 结构性预防（**比事后回源更重要**）

> 本线的教训是：134 条"未命中"是**交付前才发现**的。真正的预防是**在构造期约束**——

- **附件引文必须逐字来自候选池条目**：装配器（如 `assemble_professional_layer.py`）在写盘前**必须断言**
  "每条引文都是池条目「原文（逐字）」的逐字子串"，否则**拒绝写入**（把问题挡在源头，而不是交付前回源发现）。
- **页锚在写入时就必须是"一引用一锚＋书代号＋页/区间"**：禁止写入"多引用块"与"缺书代号锚"。
- **🆕 「引号＋引用锚 ⇒ 声称逐字」是一条强口径**（2026-09-17 加固）：附件里带引用锚的 `「…」`
  **一律视为"声称这是书里的逐字"**，必须能在池条目里逐字命中。
  · **技能自己的话不要放进带锚的引号**（如 `**本件答**「…触发句…」（锚）`）——要么去掉引号（推荐：
    去掉即不再声称逐字），要么在**当册配置**里显式声明"射程排除"并**逐条打印＋计数上限（只许减少不许增长）**。
  · 排除规则的**精度**要求：自述标记必须**紧跟引文**（同一行、紧邻）；**禁止**用"本行前 N 字 ∪ 上一行"这种宽口径
    ——实测会把**同行别处的真引文**一起排掉（`A-100`）。
- **构造期即跑** 通用闸（阶段2 收口处），而不是等到阶段5（命令见 §22.4）。

### 22.4 门禁挂载（**一闸通吃** · 2026-09-17 重写）

> **背景（缺口登记）**：本节原写「执行单已加第 37 项…供后续每本书继承」，但当时的闸
> `gate_layer_quotes.py` **把锚形态／目录／文件名／页源全部写死教育线** ⇒ 换册**空集拒跑**；
> 于是债券线手搓 8 个脚本/29KB、manias 另写一座闸——**"继承项"没有一件通用仪器能执行**；
> 同时 `gate_checklist` 的官方项数常量落后（36 vs 模板 37）⇒ 第 37 项被判"信息性不拦"、
> **没有任何闸看一眼**（本册 #37 的"完成日期"格当时是空的）。⇒ 见 `A-98`／`A-99`。

- **唯一的执行命令（任何线、任何册）**：
  `python tools\layer_quotes_gate.py --task <slug>`（**rc=0** 才算过；R1–R8 见工具 docstring）。
- **锚形态／页源模型／引文取法一律在当册配置**：`.work/<slug>/layer-quotes-<slug>.json`
  （模板 `tools\layer-quotes-config.template.json`）。**缺配置 ⇒ 拒跑（rc=1），绝不回落任何册的默认值**（`A-74`）。
- **两种页源模型**（配置 `pages.mode`）：`single_file_pagemarks`（单文件＋页标记，如 `book_text.md`／`ocr_ds.txt`）
  ／`file_per_page`（每页一文件，如 `ocr/<book>_pNNNN.txt`）——分页一律复用唯一权威
  （`verify_candidates.detect_pagemark`／`stage15_merge_task.build_page_index`），**不另写第二份**。
- **未迁移的册必须自声明**：配置里 `gate_ready=false` ＋ 写明理由与在役仪器 ⇒ 闸打印
  **"不适用（N/A · 自声明）"**并 rc=0（**是声明，不是静默通过**）。教育线（书别名锚＋1200+ 引文＋
  "未命中交判官终裁"口径）当前即此状态，由 `gate_layer_quotes.py`（R1–R8，postflight ⑧-b）把关；
  其迁移需先实现 `miss_classes`（已声明类别＋上限只许减少），**不得为变绿造类别**（`A-34`）。
- **已挂入 `tools\postflight.py` ⑧-c**（通用版）：对**每个声明了当册配置的任务**跑一次，非 0 即红。
- **官方项数只有一处权威＝官方模板**：`gate_checklist.py` 的项数**从模板自动推导**（读不到即报错，
  **不回落硬编码常量**）；执行单已含 **第 37 项**（每册继承，模板同批更新）。


---

## 23. V4.9 增补：**通用蒸馏的十条强制项**（换书 · 长会话 · 判官输入 · 成本口径 · **交付前软闸回改**）

> **为什么有这一节**：一次完整的换书蒸馏（文字版 PDF、无 OCR 档）实测暴露了**九类会静默出错的地方**——
> 它们的共同形态是「**不报错、闸也没红，但答案是错的**」，或者「**闸红了，红的却不是你**」。
> 本节把它们从"经验"升级为**命令级条款**，并给出**可复制的自检命令**。
> ⛔ **本节全程通用**：一律用 `<slug>`／`<task>`／`<技能根>`／`<册根>` 占位；**不得**写入任何一本书的书名、
> 章节名、slug、行数、页锚写法或期望件数（`A-74`：通用件不得特定化）。

### 23.1 换书「四查」（**开工即做，跨册通用**）

换一册书时，**工具链里"属于上一册的参数"是最危险的默认值**——它不报错，只产出错答案。四查：

| # | 查什么 | 为什么 | 自检命令（期望） |
|---|---|---|---|
| **1** | **页锚形态只许来自"当册配置"或唯一权威探测函数** | 曾把上一册的形态（`===== [PAGE n] =====` 式）**写死**在按页切分的仪器里；本册形态是**行首独占一行的 `[p<n>]`** ⇒ 分页全错，而**症状是"命中率照样算得出来，只是它是错的"** | 仪器必须打印**形态决议行**（形如 `页锚形态：<形态>（源＝<src>；决议自 <配置>）`）；**没有这一行 ⇒ 判为未自证** |
| **2** | **同一语义的历史配置键名必须都认** | 两册的配置分别写了 `pagemark` 与 `pagemark_form`；只认其一的仪器对另一册**静默回落**到默认形态 | 读配置处：`spec.get('pagemark') or spec.get('pagemark_form') or 'auto'`；并断言"两键都试过" |
| **3** | **门禁代跑子工具时，任务/路径类参数必须显式传** | 阶段门禁代跑候选池校验器时**漏传任务参数** ⇒ 它回落到**自己的默认任务**，**校验的是别的册，却把红判给当前册**（当前册明明 `✔ 全部通过`，闸却报红） | `python tools\gate_stage.py --task <slug> --stage 1` → 期望 `✔ 阶段1-提取`；再对**另一个已知任务**跑同一闸，确认**结论跟任务走** |
| **4** | **跨段（跨页/跨块）判定只许"精确拼接定位"** | 跨段引文在**任何单段里都找不到整条** ⇒ 只查单段的仪器一律报"找不到/错锚"；**改启发式窗口也错**（固定长度前后缀会两侧同时落空；改"最长匹配"又会被别处**更短的巧合**盖过） | 判据＝「各段归一化文本**按段序拼接** → 在拼接串里 `find` → 用**前缀长度和**把起止偏移映射回段号」；自检：`python tools\stage15_merge_task.py --task <slug>` 期望 `🔴 硬项 0`，并对历史册回归**逐键相同** |

### 23.2 「两层读数」并报（**原稿层 ≠ 在役层**）

同一批技能**同时活在两层**：`.work\<task>\skills\`（阶段2 原稿）与宿主 `<宿主>\.dsh\skills\`（在役）。
仪器若**按路径反推宿主**，原稿层会被推成 None ⇒ 凡是"引用某 slug 是否实存"的检查，**在原稿层必然报缺**，
而在役层（兄弟件齐备）**同项全过**。

- **纪律**：机器层读数**两层并报**，**以在役层为权威**；两层 `SKILL.md` 必须**逐字节一致**（写盘后逐文件 MD5 对照，不一致 0）。
- **不要**为了"让某一层变绿"去改另一层的边界字段——那会制造**副本漂移**（`A-17`）。

### 23.3 判官输入包「三自检」（**派发前逐项打勾，否则不得发出**）

判官报告里凡出现**「不可核／看不到后半段／只给字段没给原文」**，**先修工装再采信**（`A-11`／`P-23`）。
三条最常被忽视的：

1. **摘录窗口必须覆盖"整段枚举"**——按行窗口截取（如只取 8 行）会把**编号列表的后半截掉**，
   判官据此判"结构缺项"，而它其实是**完整的 7 项**。⇒ 摘录**以"段"为单位取全**，或**取下限 ≥30 行 + 断言末行不是编号中间**。
2. **标题形态必须穷举**——同一语义的小节在各册可能写作 `## X` / `### X <名>` / `### 名 X` 等；
   **只按行首匹配一种形态 ⇒ 摘录成"（未找到）"**，判官看到"结构指标 True ↔ 摘录未找到"的**自相矛盾**，据此报**假 🔴**。
   ⇒ 匹配口径＝「标题行**内**含关键字」，不要求行首。
3. **关键段落给"原文"，不给布尔**——只给 `含 CHECKPOINT: True` 时，判官**无法据原文判定**，只能给 `▲（不可自证）`。
   ⇒ 必须**内联该段的逐字上下文**（判官是**裁决者**，不是推导者）。
4. **"对照附录"不得成为提示通道**——随包附机器结论时，**凡逐题点名候选对象的一律不许附**
   （列名叫「对照／邻族风险／候选／预测」都一样）：这类列**对部分题恰好就是正确答案**，判官照抄即可"押中"，
   **盲测随之名存实亡**。⇒ 机器结论**只给聚合计数**；确需给明细的，把"逐题候选名"整列删掉。
   ⛔ **判据**：把题集的目标名与包内**每一行**比对，**命中即有通道**（**不许**用关键词扫全文——
   "本包不提供期望答案"这类**否定句**既会假红、也会漏掉真泄漏）。

> **判官输入工程**（承 §20.4）：件名↔正文**成对率 100%**、**禁泄漏期望答案**（逐行核，不靠关键词）、
> 载荷**内联**、机械层 stdout **前置**、包**分片**、判官**分段落盘**。
> 派发前自检一律跑：`python tools\check_judge_pack.py --pack <包路径> --prompts <题集.json>`（期望 rc=0）。

### 23.4 判官报告的**解析必须穷举形态**（否则对账结果毫无意义）

判官交卷的表**不止一种写法**：①`| # | id | 裁定 | 理由 |`；②`| ## id | 裁定 | 理由 |`（序号与 id 合并在同一格）；
③顶部可能留一张**空裁定**的骨架表，真正的裁定在**分批小表**里；④**"理由"列里也会出现技能名**。

- **判据（定版）**：id 从**任一格**用正则取；**裁定取"整格恰好等于"某候选名或判停标记的那一格**。
- **反例（实测自伤）**：按"整行找 id + 整行找 slug"解析 ⇒ 把**空骨架**与**理由列里的名字**都当成答案，
  把 **25/25 误报成 0/25、18/25**（连错三版）。⇒ **解析器改动后必须用"已知全对"的旧报告回归**.

### 23.5 「修工具」还是「改产物」——**三问**（本节最贵的一条）

闸报红时，**改闸永远比改产物省事——所以它永远是最危险的选项**。

1. 这条判据**有没有权威出处**？（手册明文写了的形态，就是规范；没写的才谈"口径"）
2. 我的改法**是不是在放松它**？（放宽 = 自造一把更松的尺）
3. 改完，**别的册还绿吗**？（对历史册回归；**打破别人就是改错了**）

> 实测自伤（登记于 `A-84`）：曾为了迁就技能正文里的**多引用块**写法（`（A／B／C sX～sY）`）去给仪器加
> "共享区间断言"分支。**手册 §22.3 本来明文禁止多引用块**；迁就它既**放松尺子**，又**打破历史册回归**
> （一条册的 100% 变 99.4%）。**已回退，改为改产物**（拆成"一引用一锚"）。
> **一句判据：能被"一引用一锚"表达的，就不许用"多引用共享区间"。**

### 23.6 成本「三口径并列」纪律（**通用**）

「实测成本」在不同账上会给出**不同数量级**的数，而**原始 token 可以完全一致**（⇒ 差异全在**计价模型**，不在数据完整性）。

| 口径 | 权威取值方式 | 性质 |
|---|---|---|
| **① 会话级台账**（**主口径**） | 按**会话 id** 记账的账本；开工/交付各拍一次快照，`diff` **逐会话列增量** | **唯一能剥离并发任务**：新会话＝本任务的子代理（可逐条认领）；有增量的既有会话＝并发（单列剔除） |
| ② 按天台账 | 按**天 × 模型**聚合的账本 | 与①**可差一个数量级**（计价模型不同） |
| ③ 账户余额差 | 服务商余额快照前后差 | 实际扣款参考；无法按会话拆分 |

- **纪律**：**三栏并列、不择一、不平均**；凡两口径**原始数据一致而结论差数量级** ⇒ **去查计价/判据本身，别查数据**；
  报告必须标**是否纯量**并附**快照时刻**；差额无法剥离时**如实写上"无法剥离"**。
- **纯量判定**＝**逐会话认领**（不许写"应该没别人在跑"）。
- 自检：`python tools\cost_attrib.py snapshot --label <标签>-开工` … `diff --before <标签>-开工 --after <标签>-交付`
  （内置对账行：**当日各会话之和 == 当日账合计**，不等即拒绝出结论）。

### 23.7 执行单「例外登记」的**格子语法**（**格子就是接口**）

例外登记是"不执行项"的唯一合法出口，但**解析器对格子写法有隐含要求**：

- **"不执行项#"格只写编号**，**不要**在格内写路径（路径里的 `/` 会被当分隔符，把格子切碎 ⇒ **登记不被识别**）。
- **"用户确认"格必须是肯定式**：出现 `未／待／否／TODO／pending` 一律**不算已确认**（防"静默豁免"）。
- 原因格要写**三件事**：**输入形态决定的不适用**（而非降级）＋**等价证据**＋**"不裁剪任何检查项"**。
- 自检：`python tools\gate_checklist.py --task <slug>` → 期望 `✔ 逐项完成（…，例外 N）`，且 **N 与你实际登记数一致**。

### 23.8 收尾证据**按规范名落盘**（附**来源声明**）

产物闸认的是**规范文件名**（如 `blind-machine.md`／`blind-judge-subset.md`／`darwin-judge.md`／
`engine-visibility.md`／`fair-regression.md`／`polish-scan.md`／`rquote-check.*` 等）。
手里已有实测输出但文件名不同时——**按规范名落盘是工程动作，不是造假**，前提是：

- **每份文首写明**：**来源命令** ／ **原始产物路径** ／ **时刻** ／ **任务 slug**；内容**保持原样**（不润色、不补数）。
- 可追溯性靠**来源声明**，不靠"文件名像"。

### 23.9 长会话「体积前置检查」（**goal/无人值守续跑场景的强制项**）

**症状**：本区纪律要求"**压缩后 ≈1 MB 或 ≈2,000 帧即换会话**"，但**自主续跑（goal 轮次）不会自己停下来**——
一轮一轮跑下去，会话文件持续膨胀，最终**越阈值而无人拦**（实测某次已达 **≈5 MB**）。

- **纪律**：**每一轮自主续跑开始前**先查体积；超阈值 ⇒ **落盘 + 交用户开新会话**，不得"再跑完这一步"。
- **自检**：取最近修改的会话文件大小
  `Get-ChildItem <sessions 根> -Recurse -Filter session.jsonl.zstd | Sort-Object LastWriteTime -Descending | Select-Object -First 1 Length`
  （**>1 MB ⇒ 触发换会话**；旧会话**一律保留，不删**）。
- **配套**：把"本段进度"写进 `.work\<task>\PIPELINE_STATE.md` + 讨论记录，保证新会话可无痛接续。

### 23.10 本节引入/挂载的工具（**全部通用、零书别数据**）

| 工具 | 作用 | 期望 |
|---|---|---|
| `tools\check_judge_pack.py` | **判官输入包派发前自检**（件名↔正文成对率／期望答案泄漏（逐行）／**摘录窗口是否覆盖整段枚举**／关键段落是否给原文／判停规则在不在／机械层前置） | rc=0 |
| `tools\cost_attrib.py` | 会话级成本归属（含当日自洽对账行） | 对账相等 |
| `tools\fix_anchors_generic.py` | 阶段1.5 错锚**确定性勘误**（复用合并器的判定函数，**同一把尺子**；歧义不猜） | 幂等 0 待改 |
| `tools\slice_verified_by_skill.py` | `verified.md` 按技能族切片（各片之和 ＝ 总数断言；归属缺失不静默） | rc=0 |
| `tools\coverage_by_chapter.py` | 逐章取材覆盖表（防线4 机器证据；目录页/末章外页**从配置读**；丢弃项逐条列出） | 零贡献章须有理由 |
| `tools\split_long_lines.py` | **超长行回改器**（只插换行＋对齐缩进；**逐处自证"去空白后逐字节相同"**；引文组/引号/inline code 原子不拆；表格行 SKIP 另案） | 幂等 0 待改 |
| `tools\table_to_blocks.py` | **超长表格行回改器**（表格不能拆行 ⇒ 转「逐字段标注行」；**逐字段守恒断言**：标签↔表头、值↔单元格、落盘文本↔拼接串） | 转置后管道行 ≥5 |
| `tools\polish_scan.py` | 交付前打磨扫描（超长行／空话尾巴／速查一致性／路由副本一致性）；**截断自声明＋`--all`**；**词表命中落在逐字引文内 ⇒ 豁免并自声明** | rc=0 |
| `tools\rquote_page_check.py` | 防线3「引文逐字＋页码定位 100%」；**`--include-aux` 把附件层纳入射程**（附件里的「（`id` sNN）」同样可核） | PAGE_MISS＝NET_MISS＝0 |
| `tools\layer_quotes_gate.py` | **引文型附属文件零问题闸（通用版）**：按**当册配置**读锚形态／页源模型／引文取法，对**任意册**跑 R1–R8（含射程排除带上限、归一化族、分栏自洽、空集拒跑、schema+口径） | rc=0（或自声明 N/A） |
| `tools\check_layer_sync.py` | **原稿层↔在役层逐字节同步闸**（`A-17` 多层副本漂移；同名对 MD5；临时/审计区默认排除并打印理由）｜挂 postflight **⑧-d** | rc=0（0 对漂移） |
| `tools\check_plugin_portability.py` | **插件可移植性发版闸**（A 包内无作者路径／B 有可覆盖出口／C 服从 env 根；`A-104`：作者机器上测不出可移植性）｜挂 postflight **⑧-f**，并被 `repack_plugin.py` 调用于重建后校验 | rc=0（不得发布则 rc=1） |
| `tools\check_instrument_coverage.py` | **引文面仪器覆盖对账闸**：每册要么通用闸过（`gate_ready=true`）、要么专用闸**在门禁里真有座位**（`incumbent_gate_seat` 必须存在于 `postflight.py`）⇒ 防"纸面覆盖"｜挂 postflight **⑧-e** | rc=0（0 缺口） |
| `tools\layer-quotes-config.template.json` | 上者的**当册配置模板**（锚形态／页源／引文取法／射程规则字段说明；**零册别数据**） | 每册照填 |

### 23.11 V4.9 修订记录
- **R17** 换书四查（页锚形态／配置键名／代跑传参／跨段精确拼接）——源自 `A-81`…`A-84` 的实测。
- **R18** 两层读数并报（原稿层 vs 在役层；同批次逐字节一致）——承 §19.3 四层副本模型。
- **R19** 判官输入包**四自检**（摘录窗口／标题形态穷举／关键段给原文／**对照附录不得成为提示通道**）＋ `check_judge_pack.py` 派发前闸。
- **R20** 判官报告解析形态穷举（含"必须用已知全对的旧报告回归"）。
- **R21** 「修工具 vs 改产物」三问（权威出处／是否放松尺子／别的册还绿吗）。
- **R22** 成本三口径并列纪律（会话级为主口径；纯量＝逐会话认领）。
- **R23** 执行单例外格语法（格子即接口）＋ 收尾证据规范名落盘（附来源声明）。
- **R24** 长会话体积前置检查（自主续跑前的强制项）。
- **R25** 包版本对齐规则（§19.4 R6）：4.8.0 → **4.9.0**（双 tgz 同批重打包 ＋ 双门复验）。
- **R26** H1 标题 V4.6 → **V4.9**（此前落后于增补号，属 `§19.4 R7` 所指的同类滞后）。
- **R27** **交付前软闸（d1）零缺陷 ＋ 回改纪律**（新增 §23.12；源自「逐处回改批」的 7 条实测：`A-91`…`A-97`）。
- **R28** **附件层纳入引文回源射程**（`rquote_page_check.py --include-aux`）：附件文件里的「（`id` sNN）」同样是对源文的**可核断言**；不纳入 ⇒ 附件层页锚**从未被回源**（本册实测新增 273 条断言，全绿）。
- **R29** **报告/自检器的三条自证纪律**（新增 §23.12.4）：报告**截断必须自声明**；**判据先分形态再取值**并在输出里声明形态；**shell 只做调度、判据住在文件里**（内联非 ASCII ⇒ 编码失真给 0）。
- **R30** **§22 重写：「一闸通吃」＋当册配置**（新增 §22.4）：`layer_quotes_gate.py` ＋ `.work/<slug>/layer-quotes-<slug>.json`；缺配置拒跑不回落；未迁移的册须 `gate_ready=false` **自声明**；挂 postflight ⑧-c。
- **R31** **官方项数只有一处权威＝官方模板**：`gate_checklist.py` 的 `EXPECTED_N` **从模板自动推导**（读不到即报错，不回落硬编码）；执行单 **第 37 项**纳入闸（此前被判"信息性不拦"——`A-98`）。
- **R33** **插件可移植性＝发版闸**（用户 2026-09-17 要求「以后做的插件都要自己把关：别人电脑上能不能用」）：`tools\check_plugin_portability.py` 三判据——**A 包内不得有作者机器路径**／**B 含路径字面量的脚本必须有可覆盖出口**／**C 服从性测试**（空假根下必须服从 env 或明确报错）；**判据是"服从 env 根"而不是"能不能跑"**（`A-104`）；挂 `postflight ⑧-f`，并在 `repack_plugin.py` **重建 tgz 后强制校验，不过则不得发布**。
- **R32** **「引号＋引用锚 ⇒ 声称逐字」强口径**（§22.3）：技能自己的话不进带锚引号；射程排除必须**紧跟引文**、**逐条打印**、**上限只许减少**；禁止"本行前 N 字 ∪ 上一行"的宽口径排除（`A-100`）。

### 23.12 交付前软闸（d1）**零缺陷**与**回改纪律**（**通用强制项**）

> 依据：手册 §14 软闸「d1 行文：空话尾巴／**超长行 > 260 字符**」。本条把"软闸报出来的东西"钉成
> **可执行的收尾动作**，并规定**哪些不许按提示去改**。全部来自「逐处回改批」的实测（`A-91`…`A-97`）。

#### 23.12.1 交付前**零超长行**（用户可要求"逐处回改"而非登记豁免）

- **判据**：`python tools\polish_scan.py --dir <技能根>` ⇒ **超长行 0、空话尾巴 0**，rc=0。
- **回改方式（两条，按数据形态分派——`A-07` 家族）**：
  - **文段/列表项（可插换行）** ⇒ `python tools\split_long_lines.py --dir <技能根>`：
    **只插换行＋对齐缩进**，逐处自证 **`去空白后逐字节相同`**（⇒ 逐字引文、页锚 id、候选池键全部不变）。
    `（\`ID\` sNN）` 引文组、`「…」` 逐字引文、`` `inline code` `` **原子不拆**。
  - **表格行（markdown 一行即一行，不可拆）** ⇒ `python tools\table_to_blocks.py --dir <技能根>`：
    **宽表 ⇒ 逐字段标注行**（表头列名原样搬成标签、单元格内容原地下沉、**零新增词汇**）。
    **守恒断言**：标签↔表头（列表级相等）、值↔单元格（逐字节相等）、落盘文本↔拼接串（去空白相等）。
    转置后复核 **管道行 ≥5**（`d5-c2` 可执行正证据的判据）。
- **纪律**：回改前后各跑一遍同一把仪器（`polish_scan`），并**跑下游全套**（见 §23.12.3）。
  **不许**为了让闸变绿而给软闸加"表格行豁免"这类**放松尺子**的动作（`A-34`）；确需豁免时**登记口径**并说明理由。

#### 23.12.2 **不许回改的东西**（软闸假阳性 ⇒ 修检测器，不改产物）

- 词表/正则命中的位置若落在 **`「…」`／`“…”`／`『…』` 逐字引文内**，那是**书里的原句**——
  **改它一个字即违反 `A-01`（逐字引文不得改写）**，而且**所有回源器都会照样报绿**（它们查的是池内引文）⇒ **错得无声**。
  正解：**修检测器**（引文区间豁免）＋ 豁免**必须自声明**（处数＋行号）。
- 同类：**frontmatter／description** 一律不参与排版类回改（`desc` 是 ≤1024 的硬闸对象）。

#### 23.12.3 回改后**必跑**（同批，不许留到"下次"）

1. **独立守恒复核**（不采信回改器自证）：跨"拆行＋转置"两批，用**另一套归一器**或**重建式比对**
   （从新文本反解字段、与原表逐格比对）证明**一字未多、一字未少**，且 **frontmatter 逐字节未变**。
2. **机器层 A/B**：对"改前快照 vs 现行"跑同一把仪器，逐检查项 diff ⇒ **零新增 fail**
   （快照与现行须放**同深度、同命名模式**的 A/B 目录，保证**层判定一致**）。
3. **引文两道**：`rquote_page_check.py`（默认 SKILL.md 级）＋ `--include-aux`（附件层）。
4. **YAML／占位符／引擎探针**：`yaml_check_generic.cjs`、`check_desc_placeholders.py`、`skill_probe_generic.mjs`。
5. **副本治理（顺序不能反）**：`.work 原稿 → 宿主在役`（装机器 `--apply` ＋**逐文件 MD5 不一致 0**）＋
   **语料根/盲测根副本**（`route-check-root` 这类中间语料必须**同批幂等同步**，否则门禁递归 glob 抓到旧副本 ⇒ 假红，`A-88`）。
6. **门禁电池**：`preflight` ／ `postflight` ／ `gate_stage` ／ `gate_checklist` ／ `gate_selftest` ／
   `pitfall_audit --check` ／ `check_tools_generic` ／ `scan_plugin_payload` ／ `portability_audit` ／
   `check_judge_pack`（逐包）／ `coverage_by_chapter`（防线4）。
7. **改过权威手册 ⇒ 收据失效**：必须**重生成收据**（含条款抄录）再跑 `preflight`（`gate_start` 闸1 比哈希）。

#### 23.12.4 **报告与自检器自己的三条纪律**（本轮 3 处自伤换来的）

- **(a) 截断必须自声明**：打印路径上出现 `[:6]` 这类切片，**同一行输出里必须出现"未显示 N 条"**，
  并提供 `--all` 全量开关。**读报告纪律：命中条数只看头部计数行，不数显示出来的行**（否则 49 会读成 28，`A-92`）。
- **(b) 判据先分形态、再取值，并声明所用形态**：只认一种写法 ⇒ 对**合法的另一形态**报假 🔴。
  **新闸上线前，先拿"已知合格但写法不同"的历史产物试它**；分形态时**分母必须用该形态自己的标题切分**（`A-96`）。
- **(c) shell 只做调度，判据住在文件里**：**不得**在命令行内联中文/全角符号当判据（编码失真 ⇒ **给出 0**，
  数字本身是错的、且与"数据干净"不可区分）。命令行出现"命中 0"⇒ **先用文件版脚本复核**再下"不存在"结论（`A-97`）。
- **(d) 工装自身的三条硬禁忌**（同批实测）：
  · **备份必须在被扫描根之外**（默认＝技能根**同级** `_backup_*`），备份文件名**不含 `SKILL`**；
    所有扫描器/装机器**跳过 `_` 开头目录并打印"已跳过"**（否则备份被当成产物：5 件技能变 7 件，`A-93`）。
  · **游标/边界变量不得与内层循环下标重名**（遮蔽 ⇒ 游标被重置 ⇒ **死循环**：无输出、无报错）；
    排查死循环的**第一动作是取栈**（`faulthandler.dump_traceback_later`）或**逐单位进度输出**，不是加日志猜（`A-94`）。
  · **通用件里的"册别数据"还包括文件名模式/目录名/slug**（不只书名）：一律**参数化且默认值＝原册原值**，
    换册跑"默认参数"应给出**可归因的拒绝**（带实扫计数），而不是只有一句"空集"（`A-95`＝`A-74` 第二形态）。


*蒸馏 V4.9 增补（§23）· 2026-09-17 · **通用蒸馏的十条强制项**：换书四查／两层读数并报／判官输入三自检／判官报告解析穷举／修工具 vs 改产物三问／成本三口径并列／执行单例外格语法／收尾证据规范名落盘／长会话体积前置检查／**交付前软闸零缺陷与回改纪律（§23.12：零超长行＋表格行转置＋不许回改逐字引文＋回改后必跑七步＋报告与自检器自己的三条纪律）**。**与 §0–§22 同等强制**。*

*蒸馏 V4.9.2 修订（§22）· 2026-09-17 · **「一闸通吃」与继承项纳管**：layer_quotes_gate.py（按当册配置读锚形态/页源/引文取法）取代每册手搓替身，挂 postflight ⑧-c；gate_checklist 官方项数**从模板自动推导**（此前 36 vs 模板 37 ⇒ 第 37 项信息性不拦）；执行单 **#37 纳入闸**并填真证据；新增 **「引号＋引用锚 ⇒ 声称逐字」** 强口径与射程排除的精度要求（§22.3）；**R30–R32**。*


---

## §24 异机装配与门禁分工（2026-09-19 增补 · NAS 实机 L2.5 实测固化）

> 母题：**同一个包要在 Windows／Linux（含容器）／macOS 上都跑得起来**；而"跑得起来"取决于
> **装配分工**是否清楚——哪一端跑内容、哪一端跑门禁、各自缺什么。

### 24.1 两端分工（必须写进任务交接）
| 端 | 跑什么 | 依赖 |
|---|---|---|
| **内容端**（DSH 会话／容器） | 读整书、提取器、构造、判官派单、产物落盘 | 模型 ＋ 本技能 ＋ 文档读取器 |
| **门禁端**（宿主／桌面） | `gate_start`／`gate_stage`／`gate_checklist`／`gate_selftest`／`pitfall_audit`／`postflight` 等 Python 门禁 | Python 3.8+ ＋（部分检查）node ＋ js-yaml |
**容器内没有 Python 是常态**（本次 NAS 实测即如此）。两条合法处置：① 门禁放宿主跑（推荐）；
② 在容器内自建 Python（可，但**容器重建即丢**，必须登记）。**不得**因为"容器没 Python"就宣称"闸不适用"。

### 24.2 换机四查（每次新机器/新工作区开工前）
1. **门禁配置指向对不对**：`<工作区>/.dsh/gate-kit/workspace.json` 的路径必须是**本机可见路径**
   （容器内是 `/workspace/...`；宿主是 `/volume1/...`）。同一份工作区被两端共用时，
   **用 `DSH_GATE_CONFIG` 指到各自那份配置**，不要互相覆盖。
2. **模板可达性**：`V3.1全量执行单.md` 必须能在 `<root>/tools/` 找到（解析链已参数化，见 §21）；
   找不到就**打印指引**，不得静默 fail。
3. **node / js-yaml**：`DSH_ENGINE_NODE`／`DSH_JS_YAML_DIR` 指得到就全量跑；指不到则相关检查判
   **"不适用（带理由）"**，不得裸栈、也不得假绿。
4. **产物权限**：门禁产物若写成 `600`，**别的端读不到**（本次实测：宿主读容器写的 `BOOK_OVERVIEW.md` 被拒）。
   交付类产物建议 `644`；或让读端以**同一用户身份**运行。

### 24.3 依赖闭包（发行方责任）
包内门禁 `import` 到的**本地模块**必须随包（或显式声明可选）——发版闸判据 **D-import** 已闸化。
本次实测教训：`layer_quotes_gate.py` 依赖 `verify_candidates`／`stage15_merge_task`／`verify_layer_quotes`
而这三件**当时没随包**，逼得实机会话**自建等价校验器**（那属于"发行不全"，不是使用者的错）。

### 24.4 客户端侧：明文 HTTP 与安全上下文（与操作系统无关）
`crypto.randomUUID()` 只在**安全上下文**存在：`http://localhost`／`127.0.0.1` 算，**`http://<局域网 IP>` 不算**。
DSH 客户端用它铸 RPC id ⇒ 非安全上下文下 WebSocket 建不起来 ⇒ 表现为"**工作区/数据都不见了**"（其实数据完好）。
处置优先级：**① 走 HTTPS**（反向代理 `HTTPS → http://localhost:<port>`，并**同时改写 Host 与 Origin**
为受信 authority，否则 `/api` 全 403）→ ② 回环入口（`ssh -L` 隧道）→ ③ 兜底才用 polyfill 补丁
（补丁在容器内，**容器重建即丢**，务必登记）。

### 24.5 硬编码禁令（A-74 家族）
门禁里**不得写死书树名/项目名/端口/盘符**。本次实测：`gate_checklist.py`／`gate_selftest.py`
曾写死 `投资蒸馏/三闸机器化/V3.1全量执行单.md` ⇒ 换工作区即失效（实机只能建软链绕过）。已改为解析链。

### 24.6 🆕 分发通道的**可用性是一种假设**：发得出去 ≠ 下得回来（`A-134` · 2026-09-19 真机实测）

> 母题：**"我发布了"只证明"上传成功"，不证明"用户下得到"。**

**实测（同一时刻、两台机器、三个主机）**：

| 主机 | 作者本机（Windows） | NAS（Synology） |
|---|---|---|
| `github.com:443` | ❌ TCP 超时（偶发一次 200 ⇒ **抖动**） | ❌ `curl` 超时（HTTP 000） |
| `api.github.com:443` | ✔ 通 | — |
| `objects.githubusercontent.com:443` | ✔ 通 | — |

**后果**：README 原本**主推**的固定链接（`…/releases/latest/download/<别名>.tgz`）**整条失效**，
而 npm 通道又落后版本、git 直装同样要 `github.com` ⇒ **用户无路可走**。

**三条纪律**：
1. **发布 ≠ 完成**：必须**从公网回读**自己刚发的资产（下载回来 × 比字节 × **对下载到的字节重跑发版闸**）。
2. **准备替代通道**：公开仓库可用 `api.github.com` 的资产直连（`Accept: application/octet-stream`，
   **不带这个头拿到的是 JSON 元数据**）；离线场景**留档 tgz**是最稳的一条。
3. **把"通道可用性"写进交付文档**：哪个通道在什么网络下可用、失败了改走哪条——**别让用户猜**。

**为什么这条最容易被忽略**：作者机上"上传 rc=0、Release 页面能打开"就觉得发布完了；
**而真正会失败的是用户那一侧的网络**，且症状是"**下载不动**"这种**看起来像用户问题**的形态。

---

## §25 判据语法的「单一来源」与跨册回归（2026-09-19 增补 · NAS 4.9.6 实测固化）

> 母题：**同一份合规产出，不许因为"换了把尺子"而一会儿绿一会儿红。**
> 依据：《避坑手册》`A-132`（`A-04`／`A-72` 家族的第七形态）；触发场景＝NAS 异机 L2.5 实测。

### 25.1 病灶：判据的"形态语法"被复制到多处，且各自残缺
「波段条目 id」的语法（`### {band}-NNN  [类型] [技能=…]`）曾被**内联写在 17 个脚本**里，
共 **4 种互不兼容**的写法：`[A-Za-z]\d+`（丢纯字母波段 `A..G`）／`[A-Za-z]\d*` 与
`[A-Za-z][0-9]*`（丢双字母前缀 `ST`）／`[A-Za-z]+`（**丢「字母＋数字」波段 `E1`／`T1`**）。

**症状（本次活体标本）**：册 `cn-pop-2100`（波段 `E1..E6`）候选笔记**完全合规**，
`verify_candidates` 报 `🔴 切块为空：条目正则未命中任何块（工装缺陷，不得视为通过）`，
而同册在 `gate_stage` 的格式判据下 **✔** ⇒ **一手绿一手红，判决取决于用哪把尺子**。
真机普查（5 把尺子 × 真实册）：`adhd-pro`(T1..T6) 在 ④ 下 **296 → 0**；
`guozhai-qihuo`／`manias-crashes`(A..L／A..G) 在 ① 下 **825／1004 → 0**。

### 25.2 三条铁律（**修判据前必读**）
1. **同一判据只许有一份**：形态语法一律取自单一真源 `tools\_bandid.py`
   （`BAND`／`ID`／`ID_LOOSE`／`ENTRY`／`SPLIT`／`ENTRY_HEAD`／`split_blocks()`），
   别处**只许引用**；**内联即判红**（`tools\check_bandid_single_source.py`，挂 `postflight ⑬`）。
2. **修判据要写"该形态的完整语法"，不是"比旧版松一点"**：本判据被"修"过 **4 次，
   每次都修坏另一种形态**。放宽一点 = 在另一种形态上收紧一点。
3. **回归必须跨册**：上一轮"旧册回归一致"**只跑了一册**（该册波段恰好与两把尺子同族）
   ⇒ 结论看似成立，换册即假红。**只对一册成立的回归不是回归。**

### 25.3 报错必须自证"谁错了"
空集类报错（`切块为空`／`条目 0`）**必须同时打印"实际看到的标题行"**与**期望语法**——
否则使用者无法分辨"尺子错"还是"产出错"。本次正是靠这一步才挖出全部 17 处内联。
（`A-55` 家族：漏检比误报致命；`A-28` 家族：空集不得视为通过。）

### 25.4 闸落点与自证（V4.9.7 起）
| 项 | 位置 | 判据 |
|---|---|---|
| 单源闸 | `tools\check_bandid_single_source.py` | 全 `tools\` 内联残片 **0 处**（rc=0） |
| 单源闸自证 | 同上 `--selftest` | 10 个正负样本（含"令牌贴错行不算豁免""注释里的历史错法必须放行"） |
| 形态普查器 | `tools\diag_hline_blockmatch.py` | 5 把尺子 × 6 个波段形态对照表；`--census` 出真实册条目数并排 |
| 成对差分样本 | `gate_selftest` 样本 7 | 合规 `E1-001` **必过**（rc=0／条目 1）／数字开头 id **必拦**（rc=1／切块为空）＋ 判别力自证 |
| 挂点 | `postflight ⑬` | 上述两闸各一条（`postflight` **29 → 31 项**） |

### 25.5 「源/配置」的决议也**只许有一处**（`A-133` · 同批真机抓到）

**现象**：`A-132` 修好后，NAS 册 `cn-pop-2100` 不再报"切块为空"，却换成
`🔴 合计 236 项不合格` —— **条目总数正好 236 ⇒ 每一条都不合格**。
逐文件读数：`notes_E1.md ← **DIGEST.md**`（**任务自己的产出文档**），
六波段 `引文回源命中 0/29、0/38、0/47、0/43、0/46、0/33`。

**根因**：同一文件里 **两套决议规则** ——
· 页标记形态**探测**用「候选名优先」（`book_text.md`，**对**，实测打印 `book_text.md ⇒ hline`）；
· 逐文件**校验**用「`<work>/*.txt|md` 里 mtime 最新」（**错**，`DIGEST.md` 是刚生成的产出文档）。
⇒ 探测说"源是 book_text.md"，校验拿 `DIGEST.md` 去比对引文。

**为什么这条最危险**：症状**既不像产出错、也不像仪器坏**，只是"每条都不合格"——
会诱导执行者去**改合规的产出**（改 236 条引文！），而真因只是一行决议。**假红比假绿更伤**。

**两条纪律**：
1. **同一份"源/配置"只许有一处决议逻辑**，探测与校验**共用**（否则两者互相矛盾且都不报错）。
   本工作台实现＝`verify_candidates.resolve_src()`，四级链并**逐级打印依据**：
   `--src` 显式 → `bookspec-<task>.json` 的 `src` → `<work>/notes/source/<band>-*.{txt,md}`
   → `<work>` 下候选名优先列表 → 最后才 mtime 兜底。
2. **兜底必须过内容闸**：候选**必须真的含页标记**（≥2 处，三种形态任一）。
   理由＝**真实源文一定分页**，产出型文档（`DIGEST.md`／`POOL_INDEX.md`／`judge-*.md`…）不含页标记
   ⇒ 自动排除。**用"该形态的固有性质"而不是"文件名黑名单"**（黑名单必然漏）。

### 25.6 空集/全数未命中类报错，必须**自证"谁错了"**
`切块为空`、`0/N 回源未命中` 这类报错必须同时给出：**期望语法**、**文件实测标题行**（或**实际使用的源路径**）、
以及**可执行的三步自证**。只报"N 项不合格"＝把诊断成本推给使用者（`A-55` 家族：漏检比误报致命；
`A-132`／`A-133` 两条都是靠"打印实际使用的东西"才定位的）。

### 25.7 写闸时的同批自伤（如实登记，三条）
1. **判定不能把"写下来的教训"判红**：首版连**注释**一起审 ⇒ 把"记录历史错法"的说明文字判红 4 处。
   注释／文档字符串须**放行**，但**正则字符串必须审**（正则就住在字符串里）——
   切法：只有"位于语句开头的 STRING"算文档字符串。
2. **同一行既"处理"又"判定"时，判定必须用原始行**：行级豁免令牌写在注释里，而注释已被置空
   ⇒ 豁免**永远不生效**（令牌形同虚设）。
3. **白名单令牌名不得互为前缀**：`bandid-waiver` 是 `bandid-waiver-file` 的前缀
   ⇒ 行级令牌误触发**文件级**豁免。现改为 `BANDID-OK-LINE`／`BANDID-OK-FILE`（两档，语义不同）。

*蒸馏 V4.9.7 修订（§25）· 2026-09-19 · **判据语法单一来源 ＋ 跨册回归 ＋ 源决议单一来源**：建 `tools\_bandid.py` 唯一真源并回改 **17 处**内联；新闸 `check_bandid_single_source.py`（含 10 样本自证）挂 `postflight ⑬`；`gate_selftest` 增样本 7（`E1` 成对差分）／8（单源闸自证）／**9（源决议陷阱：最新的是产出文档、真源更早 ⇒ 必须仍选真源）**；`verify_candidates` 抽出**唯一决议函数 `resolve_src()`**（探测与校验共用 ＋ 兜底内容闸"必须含页标记"）并把空集/全数未命中报错改为**自证型**（打印期望语法／实测标题行／实际使用的源＋三步处置）；`diag_hline_blockmatch.py` 重写为**形态普查器**（5 尺 × 6 形态 ＋ `--census` 真实册）；发行侧同批修三处"改了却没进包"（手写清单 → **闭包推导**＋剪枝、**补 SKILL.md 同步步**、`optional-tools.json` 与事实对齐）。**R33–R37**。

